# 2026-09-01 — SvfCore + svf_check (ADR-153; B81 inc 2, slice 1)

**What changed.** New `src/svf_core.h` (TPT SVF: LP/BP/HP, bit-exact bypass,
state-pure setParams, caller-owned slew) + `tools/svf_check.cpp` oracle-as-spec
(eight probes), registered in CMake, unwired from ./verify per the standing
ruling. B81 entry amended (slice shipped; filter-env source slot renumbered
14→18 after the ADR-149 collision). ADR-153 written.

**The oracle earned its keep on day one:** T3 caught the draft's k=2 rest
damping at −6.02 dB (critical damping, not Butterworth — k is now √2); T6's
first threshold was aspirational against a 10 Hz / Q≈20 ring (rewritten as
bounded-and-decaying windowed peaks — instantaneous samples land on arbitrary
ring phase).

**Verify.** svf_check GREEN (0 failures). `./verify fast` exit 1 on
mailbox_delivery ONLY — the two FOUNDATIONS filings (brief-note-transport,
notice-qm4-intent-bus) sit on FOUNDATIONS PR #90 awaiting the human's merge;
every code gate green, and this change touches no shipped path (core is
unwired). Red reported verbatim in-session; resolution is the human's click.
