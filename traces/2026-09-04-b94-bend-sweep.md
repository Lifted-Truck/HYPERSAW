# 2026-09-04 — B94 reproduction sweep (bendsweep_probe)

**What changed.** New diagnostic `tools/bendsweep_probe.cpp`: GlideCore-direct
sweep of spring × damping × distance curve × quantise × hysteresis ×
step-timing (3240 configs) on both lanes, plus settled-pitch/dwell and
rest-offset checks. B94 row updated with the measurements.

**Findings.** No NaN/blow-up/off-grid; display grid = live grid. (a) ζ = 0
with distance curve = 1 is undamped forever — the note cycles through steps
under quantise; curves ≠ 1 already floor ζ ≈ 0.016 (inconsistent). (b) bend
quantise "scale (drag)" transposes out-of-scale held notes −1 at rest
(ADR-111's phenomenon). (c) flat bar = slow overdamped spring + scale quantise
+ 50 c hysteresis, and B93's hold makes the knobs inert. Rulings filed: ζ
floor (ADR, parity-safe), quantise-mode ordering.

**Verify.** `./verify fast` exit 0; probe is a diagnostic, not a gate.
