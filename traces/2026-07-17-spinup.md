# 2026-07-17 — Project spin-up (initial commit)

**What changed.** Repo scaffolded from the SWARM✱ spec kit via `/spinup`:
survey conducted (9 questions, answers ratified in-session), manifest written
(`project.manifest.json`, provisional), harness installed (`.claude/` hooks +
agents + settings, `./verify` with day-zero fast gate), charter (`CLAUDE.md`),
knowledge loop (`INDEX.md`/`LIBRARY.md`, seed L0001), `traces/`, `.gitignore`,
CI workflow (inert until a remote exists). Kit docs adopted as canonical.

**Amendments applied during spin-up.** `CHANGE-NOTE-splay-legibility.md`
arrived mid-session with `swarmsaw_v2.html`; diff-verified additive (R_N order
parameter + splay visualization). SPEC §5.6 and ACCEPTANCE L0-3 amended per
the note; supersession recorded as ADR-011; spin-up ratifications as ADR-010.

**Evidence consulted.** Kit files (README/SPEC/ACCEPTANCE/ROADMAP/DECISIONS/
PRIOR-ART/PARKED), the three prototype HTMLs, `autonomous/ONBOARDING.md` Part 2,
`autonomous/kit/README.md`, `autonomous/harness/*`, distillery manifest as
reference implementation.

**Verify.** `./verify fast` GREEN pre-commit (leak gate + structure + manifest
pins + INDEX↔LIBRARY integrity). Git hash: the initial commit carrying this
trace.

**Open questions.** (1) Manifest awaits final ratification. (2) Delete
superseded `swarmsaw.html`? — human gate. (3) Register HYPERSAW in
autonomous's ecosystem tracks? — resident/governor edit, not done here.
