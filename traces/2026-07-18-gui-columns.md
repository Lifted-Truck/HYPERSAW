# 2026-07-18 — GUI v3: viz column + control column; amber circle; gated controls

**Layout (human feedback round 2).** Two-hierarchy split: all visualizers
stack in one left column (circle+meters, carpet, spectrum, XY), control
clusters flow in the right column — no mixed rows. Circle capped 232px,
carpet/spectrum 58px so the column fits the (now 980×720 default) window;
single-column reflow under 640px. Browser-verified.

**Circle cluster coloring (human question — answered yes).** Under
two-cluster topology the phase circle now colors cluster-B dots amber,
matching the carpet's convention. Viz-only; not parity-relevant.

**Conditional controls (human request).** Mode-gated visibility: HIDDEN
when structurally inert (reach↔ring, μ↔two-cluster, poles↔mean-field,
grid-cyc/beat↔tempo-grid law); DIMMED when soft-inert (basin without
gravity, dissolve without onset, glide/legato outside mono). Gates
re-evaluate on local edits and host sync.

**Per-cluster controls (human question).** Parked as PARKED #16 for the
design session: per-cluster detune/size/K-α asymmetry candidates vs the
possibility that the missing satisfaction is a legibility problem (the
α 78-86° × μ interplay IS the mode's deep control). Reference-first.

**Evidence.** GUI-only change; verify fast green; pluginval 10 SUCCESS;
auval SUCCEEDED; seals OK; installed; browser-verified at 980×720.
