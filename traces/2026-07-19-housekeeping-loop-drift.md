# 2026-07-19 — Housekeeping: knowledge loop + charter drift + preset capture

**Knowledge loop (REFLECT/WRITE).** Two new lessons + one promotion:
- L0004 candidate→CANONICAL: third independent occurrence of PR-branch/
  stacked-PR stranding (PR #38 merged into its already-merged base; GitHub
  didn't retarget; rescued by cherry-pick). Rule sharpened: prefer un-stacked
  branches off main; verify the child reached origin/main after a base merge.
- L0008 (canonical): "host feature X doesn't work" can be a per-device HOST
  toggle (Live's MPE mode) or stale scan-cache, not code — the concrete
  "gate elsewhere" branch of L0006. Three research agents proved the
  plugin/wrapper correct; the human then enabled MPE mode.
- L0009 (canonical): external design/prior-art packets mis-reference local
  ADR numbers, collide on ADR numbers, and claim absent features — grep every
  cross-ref + scope claim against DECISIONS/SPEC/ROADMAP and web-verify
  citations before merging a triage. Extends L0001.

**Charter drift.** CLAUDE.md §Domain said "three single-file HTML prototypes"
while the protected-paths line (and README) already say six — corrected to
six + listed the ported C++ cores (swarm/spectra/force/filter) and the six
verify oracle chains. Root .md files surveyed: all 12 are canonical (kit docs
+ charter + loop + untracked private notes); the recent delta/recommendation
triages already removed the free-floating drift, so no further root cleanup
needed. docs/ subtree is organized (change-notes/img/integrations/reports/
research/design/presets). Stale session task #13 (Phase 4 SPECTRA, long done)
marked complete.

**Preset capture.** The human's "Strings / Noise / Alien Metal" find (SAW,
K=−0.94 splay, q=3, gravity, α=−50°, Cauchy, full scatter) saved to
docs/presets/community-finds.md as a Phase 5 demo-set candidate with an
engineering read of why small XY moves swing it across the three timbres.

Evidence: verify fast GREEN. Docs/loop only — no code, no oracle change.
