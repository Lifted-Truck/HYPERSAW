# 2026-07-18 — ADR-037 ruled (a): P=1 gate implemented, result BIT-EXACT

Human ruling: measured equivalence now; shared-voice-path A/B follow-up
later. Implementation: spectra_check runs SwarmCore (n=5, even, cents
law, detune 0.1 -> x*10c, inert extras off) against SpectraCore (P=1,
cloud 5, cwidth 0.25, wlaw 0 -> the same x*10c), same seed, retrig off,
K=+0.8, and compares the order parameter every tick for 4 s.

Result: worst |dR| = 0. Not merely within tolerance — the dynamics are
bit-identical, because at P=1 the two references' expressions coincide
(coupling, slews, Kenv, sigma [floors inactive], rng stream formula,
draw-aligned initial phases). SPEC §2's "one engine over a kernel" is
now a THEOREM of the two ports, not an aspiration: the kernel is the
only difference. This also de-risks the A/B follow-up — a shared voice
path only has to reproduce each side's kernel rendering, since the
dynamics are already provably common.

Evidence: verify full green end to end (parity 51/51, trajectories,
state, force, spectra incl. the new gate check at dR=0).
