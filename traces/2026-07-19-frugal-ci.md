# 2026-07-19 — Frugal CI (after hitting free Actions minutes) + strictness 10

Account hit its free GitHub Actions minutes mid-month (all runs stuck 'queued'
30+ min; public repo, so account-wide block, not per-run cost). Root cost:
macOS bills 10x, full 3-job matrix on BOTH push and pull_request (2x), for
EVERY change including docs-only PRs.

Rewrote .github/workflows/ci.yml (folded into PR #53 alongside the strictness
5→10 raise, since it's the same file and CI is blocked anyway):
- paths-ignore ['**.md','docs/**','traces/**'] — docs PRs skip CI entirely.
- run once per change: pull_request for PRs; push only on main (post-merge net).
- concurrency group + cancel-in-progress.
- macOS (10x) gated to push-to-main only (post-merge); PRs run Linux verify-fast
  + the Windows build (the thing this Mac can't produce). PR-time macOS is
  redundant with the local build + pluginval 10 + auval + install.
- Windows pluginval gains --skip-gui-tests: WebView2 GUI runtime is an
  explicitly-deferred residual (charter), so don't gate on it headless; DSP/
  param/state/fuzz still at strictness 10.

Knowledge loop: L0010 (CI cost discipline) added to LIBRARY + INDEX.
The human is separately notifying the top-level automate agent to propagate
the cost lesson fleet-wide.

verify fast green; YAML parses. CI parked until the cap resets.
