# traces/ — provenance log

One entry per merged change set: what changed, why, evidence consulted,
verify result + git hash. See the provenance skill (`.claude/skills/provenance`)
and the charter's Provenance section. Append-only; never rewrite an entry.
