# 2026-07-18 — Phase 2 gate: Layer-E sign-offs recorded; close proposed

**Human verdicts (in-session).** E-1: "more or less a journey" with a
steep region flanking K=0 — PASS with caveat recorded (the squared taper
4·K·|K| concentrates authority above mid-knob; any refinement would be a
macro/mapping change, never the parity-frozen taper). E-2: PASS
("reasonable assessment" of ERB-flat register consistency). E-5: PASS
(wide→mono usable). Retrigger (PR #8 NOTE_END fix): confirmed good.

**Distribution scope resolved by inspection.** The SPEC row "bimodal
(two-cluster)" DOES exist in a reference — swarmdynamics.html rebuild()
places offsets in two bands (−0.85+0.5t / 0.35+0.5t) when the two-cluster
topology is active — so bimodal moves to Phase 3 and ports WITH the
topology, parity-anchored. clustered-pairs exists in no reference; the
human is asking the original design session to add it to the prototype
(reference-first per ADR-003/L0001); it ports with parity when the drop
arrives.

**σ clarification given.** σ is not a parameter: it is the live-measured
spread (Hz) of the actual per-voice frequencies (SPEC §6.3, ADR-004),
shown in the GUI readout; K is expressed in σ units so the phase
transition lands mid-knob at any register/law/detune.

**Status.** All Phase 2 gate items are now green or human-ratified;
gate close awaits the human's one-word ratification of the scope moves.
