# LIBRARY — durable, evidence-backed lessons (HYPERSAW)

Entry format:
`[Lxxxx] <title> | tier | added: YYYY-MM-DD | tags: … | lesson: … | evidence: … | falsifier: … | supersedes: …`

Tags: swarm-dynamics · parity-oracle · plugin-platform · realtime-perf

---

[L0001] Prototype HTMLs are the spec — amendments arrive as new files, not edits | canonical | added: 2026-07-17 | tags: parity-oracle | lesson: The reference implementations arrive/update as externally-authored drops (files.zip, swarmsaw_v2.html + CHANGE-NOTE-*.md; then swarmdynamics_v2.html + an ADR log whose numbering collided with this repo's), so "current reference" is a pointer that moves and external doc drops may collide with in-repo state. Before any parity work: confirm WHICH file is the pinned reference, diff-verify the drop is additive, renumber/merge external ADRs against the local log, and fold amendments into SPEC/ACCEPTANCE first — goldens generated from a stale reference are wrong with full confidence. Headless re-verification of a drop's measured claims is cheap (extract the core, run under Node) and caught a magnitude-vs-direction distinction once already. | evidence: this spin-up session — v2 diff confirmed as v1 + change-note additions (R_N meter, seat rings, formation polygon); SPEC/ACCEPTANCE amended accordingly. Post-ratification, v2 was renamed back to swarmsaw.html and the drop archived (ADR-012). Second independent occurrence same day: swarmdynamics_v2.html + DECISIONS_2.md arrived as drops DESPITE the ADR-012 in-repo-edits policy (external design sessions don't read this repo) — promoted candidate→canonical on that recurrence. Headless verification of that drop reproduced R_q=0.97 and the 2f0≈0.080 projections exactly, but found the ~80× grid-suppression figure harness-dependent (~5× by an independent method) — merged with the magnitude marked reference-not-blocking. | falsifier: external design sessions start consuming this repo directly (drops stop arriving), making the merge discipline unnecessary. | supersedes: —

[L0002] Acceptance values can encode an unstated measurement protocol — probe the reference before formalizing | candidate | added: 2026-07-17 | tags: parity-oracle, swarm-dynamics | lesson: When a harness check against ACCEPTANCE numbers fails on a bit-parity-proven port, suspect the measurement protocol, not the port: probe the JS reference under varied protocols (sampling density, settle windows, retrigger state) until the documented numbers reproduce, then encode THAT protocol in the check. Three L0 rows needed this: shimmer min/max only appears under dense sampling (1/s aliases it flat), dissolve's numbers describe only the initial transient, and the free-row values were snapshots of a ~1/√n fluctuation that no protocol reproduces (→ erratum, regime classification per the tolerance line). Never adjust thresholds to pass — reproduce the measurement or surface an erratum. | evidence: trajectory_check went 16/20 → 21/21 with zero threshold changes; traces/2026-07-17-phase1-swarmcore-parity.md. | falsifier: a future ACCEPTANCE revision states protocols explicitly alongside values, making probing unnecessary. | supersedes: —

[L0003] New host-side tool targets hit the same MSVC portability traps — GATE it, do not remember it | canonical | added: 2026-07-18 | tags: plugin-platform | lesson: CI builds every tool target on windows-latest/MSVC, and each new add_executable repeats the same three traps: (1) no M_PI without _USE_MATH_DEFINES — use a local kPi constant (bit twice in one day: renderer_bench, then trajectory_check); (2) -O3 is not an MSVC flag — guard target_compile_options with if(NOT MSVC); (3) the project links the static MSVC runtime project-wide (CMAKE_MSVC_RUNTIME_LIBRARY at root — clap-wrapper only sets it in its own scope, and mixing /MT with /MD is an LNK2038 wall). Before pushing any new C++ tool: grep it for M_PI, check its flags are guarded, and don't touch runtime settings per-target. | evidence: three separate Windows CI failures on 2026-07-17/18, each green on macOS first; fixed in commits for renderer_bench (M_PI + -O3), the root CMakeLists (runtime), and trajectory_check (M_PI again). | PROMOTED candidate -> canonical 2026-08-11 on a THIRD M_PI occurrence (routing_check), written by the agent that had this very entry available — the entry's own point turned on itself: a checklist is a thing you must remember to run. | falsifier: PARTLY RESOLVED 2026-08-11 for the M_PI leg — `portability_gate` in ./verify fast greps tracked C/C++ for M_PI outside comments, calibrated silent on a clean tree and firing on a planted use, turning a ~3-minute Windows CI round trip into a local grep. The -O3 and MSVC-runtime legs remain unguarded prose. Now falsifies if an MSVC-only break reaches CI WITH the gate passing. | supersedes: —

[L0004] A filed PR's branch is frozen; a STACKED PR can merge into its dead base — verify the child reached main | canonical | added: 2026-07-18 | tags: plugin-platform | lesson: Commits repeatedly miss their merge window: PR #4 merged into a stacked base after the base's own PR had merged (stranding the GUI off main); a layout+ADSR commit pushed to PR #5's branch landed minutes after the human merged it; and PR #38 (stacked with base=#37's branch) merged INTO that already-merged base — GitHub did NOT retarget the child to main after the base merged, so the sub-osc commit never reached main and needed a cherry-pick rescue. The human merges asynchronously and GitHub's stacked-PR retargeting is unreliable. Rules: (0) BEFORE pushing to any branch you did NOT create this turn, run `gh pr view <branch> --json state` and confirm it is OPEN — a merged PR's branch is frozen, and further pushes strand silently (this is the check that was skipped repeatedly); (1) once a PR is filed its branch is frozen — new work starts a NEW branch off main, even for "the same topic"; (2) prefer independent branches off main over stacked PRs; if you must stack, after the BASE merges immediately verify the child's PR retargeted to main AND that its commit is an ancestor of origin/main (git merge-base --is-ancestor / git branch -r --contains) before assuming it landed; (3) stacked PRs state their merge order in the body. | evidence: PR #4 stranding (traces/2026-07-18-gui-polish.md); PR #5/#6 miss; PR #38 stacked-base stranding → rescue cherry-pick (traces/2026-07-19-subosc-rescue-octave.md). And 2026-07-22: ~14 detune-lab commits pushed to PR #70's branch across ~10 turns AFTER #70 had merged — stranded off main until a fresh-branch rescue (PR #72). FOUR occurrences now; the failure is not knowing the rule but not RUNNING the pre-push state check (rule 0) — make it a reflex. FIFTH OCCURRENCE 2026-08-11, and the sharpest: PR #261 (base = an intermediate branch) is badged MERGED by GitHub, yet its content never reached main — the base had ALREADY been consumed by its own PR before the child landed on it, so the child merged into a dead branch and main had zero references to the fix. The audit that followed found the F2 conformance tools absent from main the same way. NEW RULE (4): A MERGED BADGE IS NOT EVIDENCE THE WORK IS IN MAIN. It reports that a PR closed against ITS OWN BASE, which may not be main. Verify by CONTENT — grep main for a distinctive identifier from the change, or `git merge-base --is-ancestor` — never by the badge, and never by the human's report of having merged it. | falsifier: a repo setting (auto-merge with required approvals, or reliable stacked-PR retargeting) makes post-file pushes and stacks safe. | supersedes: —

[L0005] Parallel key maps drift; symmetric lies evade value checks — audio is the stronger oracle | candidate | added: 2026-07-18 | tags: plugin-platform, parity-oracle | lesson: The shell kept two hand-maintained key chains (applyParam's and readParam's); adding the dynamics params to one but not the other made get_value fall through to 0 — state saved lies, and the "every value round-trips" check PASSED because both instances lied identically. Only state_check's restored-audio-bit-identical requirement caught it. Fixes: (1) single-source key maps (core.getParam over the same paramSlot table as setParam — the drift class is now unrepresentable); (2) when checking round-trips, compare against ground truth the same code didn't produce (here: rendered audio). Diagnosis pattern that worked: determinism triage (A1==A2? A==B?) then re-apply-at-own-value bisect. | evidence: 2026-07-18 state_check RED on the dynamics surface; traces/2026-07-18-dynamics-surface.md. | falsifier: a schema/codegen layer generates both maps from one table, making manual drift impossible. | supersedes: —

[L0006] A wrapper's DEFAULTS are your host-facing surface — trace the delivery path before claiming host support | candidate | added: 2026-07-18 | tags: plugin-platform | lesson: ADR-036 shipped the claim "Live via the wrapper delivers MPE as note expressions with no further surface" — false on both legs. clap-wrapper's VST3 layer advertises only the note expressions in its _expressionmap, which defaults to PRESSURE-only behind a compile flag (CLAP_SUPPORTS_ALL_NOTE_EXPRESSIONS) that make_clapfirst_plugins never even forwards; and MPE can arrive as rotating member channels + per-channel 0xE0 CLAP_EVENT_MIDI, a type the shell ignored. Implementing the CLAP side of a capability proves nothing about what a wrapped host can SEND you: the wrapper's defaults and its host-side advertisement (INoteExpressionController, IMidiMapping) gate the whole path. Rule: before claiming "host X will deliver Y", read the wrapper's source for (1) what it advertises to the host, (2) what event type it emits inward, (3) which defaults/flags gate both — and check for a plugin-side extension seam (here CLAP_PLUGIN_AS_VST3) before reaching for compile flags or submodule edits. | evidence: field report "MPE bend isn't working yet" in Live post-#27; wrapasvst3.h:474 (PRESSURE-only default), make_clapfirst.cmake:171 (flag not forwarded), vst3/process.cpp:321 + auv2/process.cpp:551 (0xE0 → CLAP_EVENT_MIDI); fixed in ADR-038 / traces/2026-07-18-mpe-wrapped-formats.md. | falsifier: human verification in Live shows bend still dead with the extension answered and the MIDI handler in place — the gate was elsewhere. | supersedes: —

[L0007] Note-lifecycle bugs localize by layered exoneration; the wrapper forwards MIDI vel-0 note-ons verbatim | candidate | added: 2026-07-18 | tags: plugin-platform | lesson: For "note doesn't stop"-class reports, exonerate layers headlessly in order — core (direct render harness), shell (seeded random note streams through the real CLAP process()), wrapper (read the translation code) — before blaming the DAW. Two durable facts from the first application: (1) the AUv2 clap-wrapper translates MIDI status 9 to CLAP_EVENT_NOTE_ON without the MIDI 1.0 vel-0-is-note-off remap, so a velocity-insensitive synth strikes an unreleasable full-gain voice on a controller's 0x90-vel-0 release — the shell must remap velocity<=0 note-ons to the note-off path (ADR-038); (2) fuzz generators that deliver events time-sorted must DRAW timestamps sorted per block, or they emit an OFF preceding its own ON — an order no host can produce — and fake the very hang being hunted (first run: 94 fake hangs, all harness artifacts). | evidence: tools/notefuzz_check pre-fix 50/50 vel-0 hangs, post-fix 75/75 clean; parity 51/51 untouched; traces/2026-07-18-vel0-noteoff-hang.md. | falsifier: upstream clap-wrapper adds the vel-0 remap (making the shell remap redundant defense), or a host is shown to legitimately send vel-0 NOTE_ON meaning a real strike. | supersedes: —

[L0008] "Feature X doesn't work in host Y" may be a per-device HOST toggle, not your code — verify host enablement before the next code round | canonical | added: 2026-07-19 | tags: plugin-platform | lesson: MPE bend was "broken" in Ableton Live through THREE rounds of plugin/wrapper code (ADR-036, ADR-038) — the actual cause was Live's per-device "MPE Settings" toggle being OFF, which makes Live send ordinary non-MPE MIDI that no plugin-side code can rescue. Before a second code round on any "host feature doesn't work" report: (1) verify the HOST's per-device enablement (an MPE-mode / note-expression / device-panel toggle) — many host capabilities are gated behind UI the plugin cannot influence or even observe; (2) rule out a stale scan-cache (the host may be testing a pre-fix bundle); (3) confirm the delivery mechanism is the one you implemented (Live's VST3 note-expression is macOS-only; Windows falls to per-channel MIDI). Background web/agent research of the host's documented behavior is far cheaper than another code+build+DAW cycle. This is the concrete "gate was elsewhere" branch of [L0006]: static wrapper analysis correctly said "plugin/wrapper is fine, look host-side," and it was right. | evidence: 3 Sonnet research agents (2026-07-19) proved the clap-wrapper advertisement correct line-for-line and SAW-mode shell plumbing clean; the human then enabled MPE mode and bend worked. docs/research/2026-07-19-mpe-vst3-findings.md. | falsifier: a host feature is shown genuinely dead with its per-device toggle ON and the plugin objectively correct — the gate really was code. | supersedes: —

[L0009] External design & prior-art packets mis-reference this repo — grep every ADR cross-ref and scope claim before merging a triage | canonical | added: 2026-07-19 | tags: parity-oracle, plugin-platform | lesson: Externally-authored packets (prior-art deltas, design recommendations, spec drops) are written by agents that do NOT read this repo's current state, so they systematically: (a) propose ADR numbers that COLLIDE with existing ones ("ADR-018" proposed twice — already the bank-renderer decision); (b) cite WRONG existing ADR numbers (a recommendation cited ADR-014 for the FDN matrix — actually ADR-030 — and ADR-010 for Daido — actually ADR-015); (c) claim features NOT in the project as shipped contributions (Arnold-tongue surface, sample-onset gravity, a grain engine — zero refs in SPEC/ROADMAP/DECISIONS); and (d) carry citations that need verification (3 bibliographic errors in one prior-art drop, caught by web-check). Triage discipline: renumber colliding ADRs to the next local slot (external-packet renumbering policy, cf. ADR-015); grep EVERY ADR cross-ref against DECISIONS.md and correct it; grep EVERY named feature/spec against SPEC/ROADMAP/DECISIONS and exclude or clearly mark anything absent; web-verify citations before they enter a protected doc; merge only the accurate in-scope core, archive reasoning to docs/, delete the source packet. | evidence: 2026-07-19 triages — PRIOR-ART delta (Lem lineage kept; Arnold/grain/sample-onset excluded; 3 citation errors fixed by verification agents) and the coupling-topology recommendation → ADR-045 (two wrong ADR cross-refs corrected). traces/2026-07-19-priorart-triage-readme.md, 2026-07-19-adr045-coupling-axes-triage.md, 2026-07-19-priorart-citation-verify.md. Extends [L0001] (which covers reference-HTML drops + ADR collisions) to design/prior-art packets and their cross-ref + scope + citation errors. | falsifier: external packets start citing this repo's live ADR numbers and shipped-feature set correctly (authoring agents gained repo access). | supersedes: —

[L0010] CI cost is a resource to manage — macOS bills 10x; skip docs, dedup triggers, prefer local verification | canonical | added: 2026-07-19 | tags: plugin-platform | lesson: A PR-per-change cadence running a full 3-job matrix (Linux + macOS + Windows) on BOTH push and pull_request drained the account's free GitHub Actions minutes mid-month — macOS runners bill at 10x the standard rate, and docs-only PRs (roadmap/traces/prior-art) triggered full builds for nothing. When it happens, everything sits 'queued' indefinitely (an ACCOUNT-wide block; a public repo's per-run minutes are free, so long queues with nothing starting = spending-limit/incident, not runner scarcity — and strictness/step content NEVER affects queueing, only the runs-on label does). Cost-discipline config: (1) paths-ignore ['**.md','docs/**','traces/**'] so docs skip CI; (2) run once per change — pull_request for PRs, push only on main (post-merge net), not both; (3) concurrency group + cancel-in-progress; (4) put the 10x macOS job post-merge-on-main only when the maintainer already builds+validates macOS locally every PR — keep the cheap Linux verify + the can't-do-locally Windows build on PRs. The local ./verify full (9 oracle chains + pluginval 10 + auval + install) is a STRONGER gate than CI; CI's unique value is the Windows build this Mac can't produce. | evidence: 2026-07-19 — account hit free-tier Actions limit; PRs stuck 'queued' 30+ min on a public repo; frugal ci.yml shipped in the same PR (#53). | falsifier: the account moves to ample/unlimited minutes or self-hosted runners, making the frugality unnecessary. | supersedes: —


[L0012] Bit-parity has a PHYSICS domain limit — the reference-only 1-ULP perturbation test is the discriminator between "port bug" and "impossible in principle" | candidate | added: 2026-07-23 | tags: parity-oracle, swarm-dynamics | lesson: When one parity scenario fails by ~5 orders of magnitude while its neighbours pass at rms 0, the established causes are protocol mismatch (L0002) or a genuine port bug — but there is a third: the REGIME ITSELF is chaotic, and no implementation could pass. The decisive test costs one render and involves no C++ at all: perturb the JS reference's initial state by 1 ULP and re-render JS-vs-JS. If that self-divergence matches the C++-vs-JS rms and GROWS over the render (positive Lyapunov exponent), sample-exact parity is impossible in principle — Math.sin and std::sin are not identically rounded, and the dynamics amplify the last bit. Bracket first (divergence should scale with the destabilising params — coupling × spread here) so the ULP test confirms rather than excuses. Resolution is never a loosened ε: remove the golden, cover the regime with a behavioural anchor (bounded, NaN-clean, the qualitative claim the param exists to make, checked with margin), and probe the anchor's own regime — a K where coupling lifts R by 0.006 is chaotic jitter that could flip sign on a legitimate refactor; anchor where the effect is unmistakable. The evidence standard (bracketed curve AND reference-only perturbation, never a bare "just floating point") is now codified in ACCEPTANCE §L0-1's Domain limit clause. | evidence: ADR-065 / traces/2026-07-23-fold-harmonic-law.md — harm-sync failed at rms 9.736e-2; JS-self 1-ULP gave 9.383e-2 growing 5.62e-2→1.147e-1; stable regimes 1.869e-19 and exactly 0; bracket 3.7e-19→4.4e-13→3.5e-07→9.7e-02; anchor retargeted K 0.6→0.9 (ΔR 0.006→0.462 vs 0.2 gate). | falsifier: a regime flagged chaotic by the ULP test later renders sample-exact across independently-compiled implementations, or the divergence is traced to an actual expression difference — then the test produced a false "chaotic" verdict and the diagnosis procedure is unsound. | supersedes: — (refines L0002 with a third cause)

[L0013] Lab → reference → core folds cross INDEPENDENT NAMESPACES — grep the destination for every identifier AND enum index before transcribing | candidate | added: 2026-07-23 | tags: parity-oracle, plugin-platform | lesson: The audition lab names its params and numbers its menus for its own UI, with no knowledge of the core's members or enum tables — so a literal transcription can collide silently in two ways: (1) NAME collision — the lab's `reach` (harmonic-law top rung) vs the core's existing `reach` (ring-topology radius, ADR-030): a duplicate member with a clashing meaning in the same struct; (2) INDEX collision — the lab's law 3 (stretch) vs the core's law 3 (tempo grid, ADR-022): transcribing the lab's number would have OVERWRITTEN the tempo-grid law, and dyn-grid (which selects law 3 via coreToDyn) would have rendered an inharmonic stack while still called a grid — plausibly staying green. Rule: before folding any lab identifier, grep the destination for the bare name and the enum value; on collision, rename/renumber AT THE FOLD (harmReach; law 5) and record the lab↔core mapping in the ADR and in BOTH law tables, so the next transcription doesn't re-derive it wrong. The lab is an instrument to play, not a naming authority — never renumber the lab to match. | evidence: ADR-065 (reach→harmReach) + ADR-066 (law 3→5) — two collisions in one seven-fold campaign, each caught by grepping swarm_core.h before the edit. | falsifier: lab and core identifiers become single-sourced (generated from one table), making collisions structurally impossible — then the grep discipline is obsolete. | supersedes: —


[L0015] A server started in the agent's shell sandbox is NOT reachable from the human's own browser — for self-contained HTML, skip the server and open the file | candidate | added: 2026-07-28 | tags: plugin-platform | lesson: Serving a lab over `python3 -m http.server` worked perfectly in the in-app browser pane and was completely unreachable for the human, who reasonably concluded the port was already occupied. It was not: the bind succeeded and was the only listener on that port. The cause is that Bash tool calls run inside a sandbox, so a socket bound to 127.0.0.1 there is on a different loopback than the host's browser sees. The in-app pane shares the agent's environment and therefore cannot detect the problem. Two consequences: (1) DO NOT use the in-app pane rendering a page as evidence that the human can see it — those are different networks; (2) for a self-contained HTML artifact (all script/style inline, no fetch, no modules) there is no reason to run a server at all — `open <file>` in the host browser is simpler, has no port to collide, and works first time. If a server genuinely is needed (module imports, fetch, CORS), start it with the sandbox disabled so it lands on the host loopback. SECOND, INDEPENDENT TRAP found the same round: a quick `http.server` at the repo root serves `.git/` and every source file over HTTP. Localhost-only bind limits the blast radius but does not make it acceptable — scope the server to the directory being shown (`--directory docs/design`) as a matter of course. | evidence: 2026-07-28 session — server on 127.0.0.1:8173 verified as the sole listener and serving the correct file, rendered correctly in the browser pane, unreachable for the human; `curl /.git/config` returned 200 at repo root and 404 after scoping; resolved by `open docs/design/shape-lab.html` with no server. | falsifier: a sandboxed Bash-started server is shown to be reachable from the host browser on this setup — then the sandbox does not isolate loopback and only the directory-scoping half of this lesson stands. | supersedes: —

[L0016] CALIBRATE THE DETECTOR BEFORE TRUSTING IT — run any new defect metric on a known-CLEAN and a known-BAD signal first; a metric that does not report ~zero on the clean case is measuring the signal, not the defect | canonical | added: 2026-07-28 | tags: parity-oracle, realtime-perf | lesson: THREE TIMES IN ONE SESSION I built a measurement instrument, got numbers that looked reasonable, and was measuring the wrong thing entirely. (1) An aliasing metric summed every non-harmonic FFT bin — it was accumulating Hann-window leakage from a dozen strong harmonics across thousands of bins, and reported a textbook-clean polyBLEP sawtooth at -27.7 dB instead of -149.5 dB. (2) A formant locator detrended the spectrum by +20log10(k) to cancel the saw's rolloff — that tilt over-weights the top octaves so hard it finds noise, and it reported 3190 Hz for an 800 Hz target. (3) A click detector thresholded |x[n]-x[n-1]| — on a 1200 Hz sine that is just the slope (~0.17/sample at 44.1 kHz), so it reported 2700 "jumps" in a perfectly clean signal, and I nearly chased a bug that did not exist. THE COMMON SHAPE: a threshold on the WRONG DERIVATIVE (or the wrong domain) measures the signal itself rather than the defect. WHY IT SURVIVES SCRUTINY: in all three cases the RANKING between configurations stayed roughly correct, so A/B comparisons still "worked" and nothing looked broken; only the absolute value was absurd. What saved me each time was noticing the absolute number was implausible against a reference I happened to know (a clean polyBLEP saw is ~-150 dB, not -27). That is luck, and it fails silently whenever I do NOT already know the right answer. THE RULE, which is cheap because both cases almost always already exist: before trusting a new detector, run it on (a) a signal known to be CLEAN — it MUST report ~zero, and if it does not, it is measuring the signal; and (b) a signal known to be DEFECTIVE — it MUST report large. Only then interpret real results. Corollary: plausible rankings are NOT validation. Domain-general — applies to any residue, noise floor, crosstalk, error rate, or A-minus-B metric where B is orders of magnitude larger than A. THE SPECTRAL CASE, absorbed here: when measuring a small quantity beside a large one, never define the small one as "total minus large" — find a place in the domain where the large quantity is structurally ABSENT and measure there. Summing every non-harmonic FFT bin measures Hann-window LEAKAGE from a dozen strong harmonics across thousands of bins, not aliasing; sampling at inter-harmonic midpoints ((k+0.5)*f0), where the nearest harmonic is hundreds of bins away, measures the residue. Applies to spectral residue, noise floors, crosstalk, and any A-minus-B metric where B is orders of magnitude bigger. | evidence: 2026-07-28 shape-lab session, all three instances measured and corrected: aliasing -27.7 -> -149.5 dB after inter-harmonic midpoint sampling (and it then revealed an 87 dB spread the broken metric had compressed to 15 dB, incl. the fractional-sync hot spot it hid entirely); formant locator replaced with a 1/3-octave envelope peak; click detector replaced with 2nd-difference outliers vs the robust local median, which then correctly read 0/s on integer sync and 300/s on ratio 4.37. | falsifier: a detector that passes the clean/defective calibration pair is later shown to be measuring the wrong quantity anyway — then calibration is insufficient and a stronger validation (e.g. an independently-derived reference implementation) is required. | absorbs: L0014 — the spectral case; consolidated 2026-08-11

[L0017] Detector calibration is per SIGNAL CLASS, not once per detector | tier: canonical | added: 2026-07-29 | tags: measurement, oracle, dsp, epistemics | lesson: L0016 says calibrate a new defect metric before trusting it. The unstated half: a calibration certifies the detector FOR THE SIGNAL CLASS IT WAS CALIBRATED ON. Carrying a validated detector to a different class silently re-opens the hole. Re-run the known-clean and known-bad cases every time the signal under test changes character (broadband -> harmonic, steady -> transient, mono -> stereo). | evidence: 2026-07-29 reverb stereo work. A sparse probe-frequency (Goertzel sweep) HF/LF metric was calibrated on the reverb tail and read exactly 0.00 dB on two independent mono configurations — genuinely valid there, because the tail is broadband. Reused unchanged on the DRY saw swarm it reported 4.62 dB of tilt at n=7 and -10.76 dB at n=8; the probe grid was landing on harmonics of one detuned voice and missing another's. Rebuilt as a Welch-averaged FFT integrating every bin per band (identical L/R -> 0.000 dB; known 6 dB HF lift -> +9.79 dB), the true worst was 0.65 dB across n=2..12. I had already written and measured a "fix" for the phantom defect before catching it. | RECURRED TWICE 2026-08-03 (promoted candidate->canonical on independent recurrence): building a click detector for the FX comb, I twice compared the wrong classes. First the floor was a DRY SINE (0.0078/sample slope) against a RINGING COMB, whose resonances legitimately exceed it — the bound was meaningless and the correct fix 'failed' it. Then two different NOTE PATTERNS, which differ in line count and entry time, so it measured their configurations rather than the click. Both were fixed by pairing WITHIN one run — worst jump near a note-on vs far from one, same signal, only position differs — which separated 1.01 (fixed) from 16.96 (planted bad). Three occurrences now, in three different domains. | falsifier: a detector that stays accurate across signal classes without recalibration — e.g. one whose calibration cases already span the classes it will meet. The rule is about untested transfer, not about detectors being fragile.

[L0018] A symmetric phase-warp axis is spectrally SIGN-BLIND | tier: candidate | added: 2026-07-29 | tags: dsp, spectra, design, measurement | lesson: For any waveshaping expressed as a monotone phase MAP, the map at +d and the map at -d are time reversals of each other, and a magnitude spectrum is invariant under time reversal — only the phase flips. So a bipolar warp control has a one-sided magnitude response, and any per-voice DISPERSION symmetric about zero folds onto |d|: half the intended spread lands on top of the other half and voices meant to differ are identical. Check the sign symmetry of any new bipolar spectral axis BEFORE building a dispersion on top of it. | evidence: 2026-07-29 HYPERSAW ensemble lab. The two-slope phase warp behind "spectral smear" measured bit-identical harmonic levels at warpD +1 and -1 (h1..h16 diffs equal to 0.1 dB across the board). Combined with the warp being a monotone 2.73 dB rms TILT rather than a resonant peak, this explained three independent human reports of total inaudibility, and the axis was retired rather than tuned. | falsifier: a phase-warp family whose +d and -d magnitude spectra measurably differ — which requires the map at -d NOT to be the time reversal of the map at +d (e.g. an asymmetric or non-monotone map). The rule is about time-reversal-paired maps, not about all bipolar controls.

[L0019] Humanised timing is CORRECTED error, not independent jitter | tier: canonical | added: 2026-07-29 | tags: timing, perception, humanize, midi, ensemble, doctrine, cross-project | lesson: Listeners judge ensemble togetherness from the SERIAL STRUCTURE of onset asynchrony, not from its variance. Independent per-note random offsets — what essentially every "humanize", "groove", or "feel" control does — produce the right variance with the WRONG autocorrelation, and read as sloppy rather than human. Model mutual error correction instead: each part nudges its offset toward the group's mean offset and then adds fresh motor noise, `off_i <- off_i - alpha*(off_i - mean_off) + noise_i`. The human-like lag-1 autocorrelation is then EMERGENT, not dialled in. Two design consequences worth more than the formula: (1) the musically meaningful control is alpha (the correction gain — how hard players pull toward each other, i.e. "tightness"), not the noise amount, which is why humanize knobs calibrated in milliseconds feel wrong at every setting; (2) alpha = 0 is an independent random walk that DRIFTS without bound, and alpha = 1 is rigid quantisation, so the usable musical range is strictly interior — a humanize feature with no correction term has no tightness axis at all, only a sloppiness axis. Applies to any onset-timing humanisation on MIDI or audio: DAW humanize, sequencer swing/feel, drum machines, sample libraries, unison/ensemble patches, multi-agent performance timing. | evidence: Wing, Endo, Bradbury & Vorberg (2014), string-quartet timing. At MATCHED variance, asynchrony detection threshold moved 64.3 -> 18.2 ms^2 purely as a function of serial structure; lag-1 autocorrelation 0.84 vs 0.39, p<0.0005 — i.e. structure dominated variance for the perceptual judgement. Surfaced in HYPERSAW's 2026-07-28 orchestral-ensemble research pass (104 agents, 3-vote adversarial verification) and implemented as `EnsembleTiming` in docs/design/ensemble-lab.html. Independent corroboration of the mechanism from the other direction: a Kuramoto phase coupling K is formally the same error-correction gain, so a coupled-oscillator architecture generates this timing natively rather than needing a bolted-on humaniser — the strongest architecture/evidence match the project has found. Recorded as canonical on human review (2026-07-29) rather than on a second occurrence. | falsifier: a listening test in which matched-variance onset jitter with human-like lag-1 autocorrelation and with zero autocorrelation are indistinguishable; or a humanize implementation whose perceived musicality tracks jitter magnitude alone with serial structure held irrelevant. Note the claim is about PERCEIVED ensemble togetherness — it does not assert that variance is inaudible, only that structure dominates it.

[L0020] A BUILT ARTIFACT MUST DISPLAY ITS OWN PROVENANCE — "am I running what I just built?" is a question the artifact answers, not the human | tier: canonical | added: 2026-07-31 | tags: build, caching, provenance, debugging, doctrine, cross-project | lesson: Any artifact a human evaluates through a SEPARATE RUNTIME — a plugin loaded by a DAW, a page served to a browser, an app installed to a system path, a container pulled by an orchestrator, a model served behind an endpoint — sits behind at least one cache the human cannot see. When behaviour does not match the source, "is it stale?" must be answerable BY LOOKING AT THE ARTIFACT, not by forensics. Stamp the build identity INTO the artifact, derived from the VCS at build time (never hand-typed: a hand-maintained version lies exactly when it matters, because the moment you forget to bump it is the moment you are debugging), mark a dirty working tree distinctly (e.g. a trailing "+") so an uncommitted local edit can never masquerade as the committed hash, and DISPLAY it where the human already looks. For self-contained artifacts with no build step, compute a fingerprint AT LOAD from the artifact's own content — it cannot go stale by construction, because it describes the bytes actually loaded rather than what someone remembered to type. Corollary: also stamp the SOURCE-of-truth question — when the human is looking at one copy while you describe another (a branch checked out before a merge; a doc regenerated from newer data), the cheap fix is a visible identity, not more explaining. | evidence: 2026-07-30/31 HYPERSAW, THREE distinct staleness incidents in two days, each costing a full debugging round: (1) merged CLAP params invisible in the DAW — the merge changed source, the loaded binary was the old install; (2) "the tool is appearing cached again" — the human's working tree was on a branch cut BEFORE the relevant merge, so their file genuinely lacked the feature I was describing; (3) reverb-lab edits invisible — genuine browser cache of a file:// page. Fixes that ended it: labs gained a git-derived `rev` timestamp PLUS a load-time content fingerprint (hash of the page's own inline scripts — maintenance-free, cannot be stale); the plugin gained a CMake-injected git short hash with a dirty-tree "+" displayed in the GUI corner. Before the stamps, every occurrence required me to run `strings`/`ls -l`/log inspection to answer a question the human should have answered in one glance. | falsifier: an artifact whose runtime provably cannot cache (no install step, no separate process, content re-read on every evaluation) — there, a stamp is noise. Also falsified if a project's staleness incidents are traced to something a stamp cannot expose (e.g. a correct binary loading stale STATE); the rule is about identity of the ARTIFACT, and state provenance is a separate problem.


[L0022] A rejected best-effort push must be RETRIED, never assumed delivered | tier: canonical | added: 2026-08-03 | tags: api, realtime, events, plugins, debugging, doctrine | lesson: When an API hands back a success flag (try_push, try_send, non-blocking write, ring-buffer enqueue), it is telling you the call CAN fail under load. Ignoring the flag and retiring the local state anyway converts a transient, fully recoverable condition into PERMANENT silent data loss. The failure is near-invisible: the rejecting path only triggers when the channel is busy, so it presents as rare, load-correlated flakiness rather than a reproducible bug, and every low-load test passes. Rule: retire local state only on ACCEPTANCE; keep the item queued and retry next cycle. Corollary for the test: a rejecting stub belongs in the harness — and must be proven to FAIL against the pre-fix code, or it proves nothing. | evidence: HYPERSAW 2026-08-03. Stuck notes survived four rounds of fixes (deferral, emit-on-release, tag retirement, envelope investigation). The core was proven innocent by measurement (post-release tail 46.4 ms at every K setting and both rtone extremes). The actual bug: emitNoteEnds ignored `out->try_push`'s return and retired the note tag regardless; the host's output buffer is finite and drainQueue floods it with param events on every knob move, so sweeping a control could crowd out a NOTE_END, which was then destroyed forever and the host withheld retriggering that pitch. The human's clue — "stalls most when I've recently changed the K value" — was what localised it, since knob movement is exactly what fills the buffer. endprobe now refuses 40 pushes; measured old code 0 ENDs delivered, fixed code 1. | falsifier: an API whose documented contract guarantees acceptance (an unbounded queue, or a blocking write) — there the flag is informational and may be ignored. The rule binds wherever the channel is finite or the call is documented as best-effort.

[L0023] A parameter range widened without its UI control ships an INVISIBLE feature — and no oracle can see it | tier: candidate | added: 2026-08-03 | tags: interface, testing, oracle, gui, plugins, doctrine | lesson: when a feature adds values to an existing parameter's range, the change is split across two files — the param definition and the control that offers the choices. Widen only the first and the feature is fully implemented, fully tested, automatable by a host, and absent from the product. Every audio/behavioural oracle passes, because the audio path IS correct; the defect lives entirely in what the user can reach. Treat "widen a range" as an atomic edit spanning param + control, the same way an INDEX pointer must land with its LIBRARY entry. Corollary: an inert-by-default parameter needs a gate proving the knob is CONNECTED, since parity staying green proves only that the default changes nothing. | evidence: HYPERSAW ADR-071 widened FX slot types 0..3 -> 0..5 to add Comp and Comb; the four GUI dropdowns in gui.html were never widened with them. Both effects shipped, ran, and were host-automatable while being unselectable in the plugin's own panel — for two weeks, with all ten oracle chains green and parity 147/147. Found only because the human MISremembered the comb as lab-only and I checked the claim instead of accepting it. The follow-on fold (comb resonance, ADR-080) therefore shipped with waveshape_check T7 measuring ring time 99/190/772 ms across the knob's range, specifically to prove the new control reaches the DSP. | falsifier: a codebase where the control surface is GENERATED from the param definitions, so the two cannot disagree — which is the real fix, and makes the lesson an argument for that generation rather than for vigilance.

[L0024] A result landing right at its threshold is evidence the DETECTOR is wrong, not that the fix is marginal | tier: candidate | added: 2026-08-03 | tags: measurement, oracle, epistemics, doctrine, cross-project | lesson: a real fix and a real defect usually differ by a lot, because they differ in KIND. When a metric reads just inside its bound, the likeliest explanation is not "the fix barely worked" but "this measurement is dominated by something other than the effect" — a confound, a mismatched baseline, the wrong window. Treat a marginal pass as a prompt to re-derive the measurement, never as permission to ship. The comfortable reading (it passed) is exactly the one to distrust. | evidence: HYPERSAW comb declick, 2026-08-03. The second detector version read 1.56x against a 1.6x bound and was GREEN. Rather than ship, I re-derived it: the two runs being compared had different line counts and entry times, so the number was mostly configuration difference. Re-pairing the comparison within a single run (near a note-on vs far from one) gave 1.01 for the fix and 16.96 for the planted-bad — a 17x separation where the marginal detector had shown 1.56x. The fix had been essentially perfect the whole time; only the measurement was marginal. | falsifier: a domain where effect sizes genuinely cluster near the acceptance threshold (incremental performance tuning, statistical A/B at low power), where marginal readings are the norm and this heuristic would cause endless re-derivation.

[L0025] A narrative decision log records WHY but not WHAT IS OPEN — it needs a separate status index, updated atomically | tier: candidate | added: 2026-08-03 | tags: docs, process, roadmap, doctrine, cross-project | lesson: append-only design documents are excellent at preserving reasoning and structurally bad at reporting current state, because an item's status lives inside the paragraph that created it and is never revisited when the world changes. The failure is silent and one-directional: entries drift toward reporting things as MORE open than they are, since closing something rarely prompts anyone to edit the paragraph that opened it. Keep a compact register of open items alongside the narrative, and require it to be updated in the same change that changes an item's status — the same atomic-write rule a knowledge INDEX needs against its LIBRARY. | evidence: HYPERSAW ROADMAP.md, 1216 lines, reconciled 2026-08-03. Its status block still described ADR-037's P=1 gate as "still open" two weeks after the human ruled it (2026-07-18, option (a)); listed already-completed branch pruning as pending; and a task tracked as open for weeks turned out to be substantively done, its three required decisions each recorded INSIDE the ADR that made the change rather than as the three standalone documents the task expected. None of these were noticed by anyone reading the document, because nothing in it invites re-reading old paragraphs. | falsifier: a project small enough that the narrative IS the index, or one whose tooling derives status from machine-readable state (issue tracker, checkboxes parsed by CI) so the prose cannot drift from it.

[L0026] A setup-time callback that reaches FORWARD into not-yet-initialised state fails silently and totally | tier: canonical | added: 2026-08-04 | tags: javascript, gui, labs, debugging, tooling | lesson: the common "wire a control and invoke its callback once to sync the display" idiom runs that callback DURING script evaluation. Anything the callback touches must already be initialised — and `const`/`let` declared later in the same file are in the temporal dead zone, not merely undefined. The resulting ReferenceError aborts the remaining top-level script, so a page loads looking almost right (markup renders, CSS applies, controls move) while every listener, timer and canvas registered after the throw silently does not exist. Declare draw/timer state ABOVE the wiring, or defer the initial sync to a microtask. | evidence: HYPERSAW labs, 2026-08-04 — twice in one session. bend-lab.html: wire() called recalc(), which touched canvas contexts declared further down; the first wire() threw and killed gestures, keyboard and the draw loop, and the only visible symptom was em-dashes where meters should be. filter-lab.html, same day and knowing the trap: wire() called schedule(), which touched a `let pending` declared below it — identical failure, again invisible in a screenshot. Both were found by checking the DOM/state programmatically rather than by looking at the page; the second was located by re-eval'ing the script body in a try/catch to recover the line. | falsifier: RESOLVED 2026-08-06 — the falsifier named tooling as the fix, and the tooling now exists: `tools/labharness/lab_load_check.mjs`, wired into `./verify fast` (human-approved gate change), executes every lab's <script> blocks under stub DOM globals and fails on any load-time throw. Calibrated by re-injecting BOTH historical bugs: the mod lab's `mtx` TDZ ('Cannot access mtx before initialization') and the bare `SR` typo ('SR is not defined') are each caught, an untouched control passes, and all 12 labs are green. The lesson now falsifies if a load-killing lab bug reaches review WITH the gate passing — i.e. if executing under stubs stops resembling a browser load. | supersedes: — (nothing superseded; escalated candidate -> canonical on the FIFTH occurrence, 2026-08-06, written by the agent that had just documented the trap, which is the strongest possible evidence for the original falsifier's claim that care does not scale)

[L0027] Infrastructure before instrument: audio context, routing, and every cross-cutting axis FIRST | tier: candidate | added: 2026-08-07 | tags: architecture, sequencing, synth, doctrine, cross-project, HIGH-PRIORITY | lesson: for an ambitious instrument (multiple oscillators/engines, a morph system, preset tiers, an FX rack), build the AUDIO CONTEXT — the mixer/master page and the routing topology — before doing major work on the sound sources themselves, and settle every cross-cutting axis the same way. The boundary between "belongs to one source" and "belongs to the patch" cannot be decided correctly in the abstract: this project classified 31 params as global by reasoning in an ADR and got 13 wrong (width, mono, inertia, the amp envelope, glide — each oscillator's copy existed but was unreachable), an error the mixer page would have made impossible because a channel strip DECIDES what is per-source. The same sequencing applies to: (1) the param-id namespace (append-only; capacity IS the stride — this project shipped a scheme whose osc-0 block was full on day one and could amend it only because no host had seen the ids); (2) preset tiers (the per-source tier fell out of the id scheme's key prefixes almost free — a format designed before the namespace would have fought it); (3) cross-source coupling semantics (a "master K" that shares a parameter vs a `link` that shares dynamics are different features; naming them requires knowing the routing); (4) global macros (a time-scale macro needs every time-domain param flagged AT INTRODUCTION — auditing later took a scan plus hand-filtering); (5) provenance/ownership display (corner colouring needs a single vocabulary defined before three UIs each invent one). The general form: any axis that CROSSES sources — audio, ids, presets, coupling, macros, display vocabulary — is cheap to define before the second source exists and expensive-to-impossible after, because the first shipped build freezes it. | evidence: HYPERSAW 2026-08-06/07 — ADR-082 Amendment 1 (stride full on day one, amendable only pre-exposure); the 13-param misclassification audit (ROADMAP § Re-order, human-directed reorder to mixer-first); preset tier landing "nearly free" from the id scheme (PR #217/#219); K-vs-link distinction (PR #222). | falsifier: a project that builds sources first and retrofits the audio context WITHOUT either breaking saved sessions or freezing wrong boundaries permanently — or one where the mixer-first order itself caused comparable rework (e.g. a channel-strip abstraction that the eventual sources could not fill).

[L0028] Consumers address a ROLE resolved at read time, never an instance captured at wire time | tier: canonical | added: 2026-08-09 | tags: architecture, gui, viz, indirection, doctrine, cross-project, HIGH-PRIORITY | lesson: every consumer of engine state — visuals, meters, labels, preset scopes, mod destinations — must name a ROLE ("the oscillator being edited", "the master bus", "the focused voice") and resolve it through a documented indirection ON EVERY READ. Binding a consumer to a concrete instance at wire time is invisible while there is only one instance and silently wrong the moment there are two, because the wrong answer is a PLAUSIBLE one: the display keeps working, it just describes the wrong object. Three incidents in one week, same shape: (a) publishViz built every per-swarm visual from `core` — oscillator 0 — so the phase circle described osc 1 no matter which tab was active; (b) the XY pad sent raw base param ids (4/6) rather than effective ids, so both axes wrote oscillator 0 while the panel repainted over the evidence, producing an asymmetric "detune works, K doesn't" illusion; (c) once the indirection existed it was inert for two PRs because its webview binding was never registered — a callback with no bind is invisible to the compiler, so the feature shipped dead and green. Three corollaries: (1) the indirection must be ONE named thing (an index, a handle) that every consumer reads, not per-consumer plumbing — the second copy diverges; (2) THE LABEL IS PART OF THE MECHANISM: a role-addressed display must render which instance it resolved to, because otherwise a mis-resolution is unobservable by eye and the bug survives every "looks fine" inspection — in this project the missing label is precisely why (a) and (c) each survived a merge; (3) identical defaults make role errors invisible, so a role-addressed view needs at least one instance-distinguishing datum on screen (level, name, index). | evidence: HYPERSAW 2026-08-07/09 — PR #227 (publishViz + XY hardwiring), PR #239 (the unregistered hzSetVizOsc bind, found only when the human reported the viz "still tacked to osc 1"), and the human's framing: visuals should never tack to a specific object but to "a category open to be negotiated via plumbing".  PROMOTED candidate -> canonical 2026-08-09, same day, by a second INDEPENDENT occurrence in an unrelated subsystem: voice lifecycle, not visuals. Eight sites in the CLAP event loop addressed `core` (an alias for oscillator 0) where the intent was "every oscillator" — PRESSURE fanned out but TUNING did not (a bend split the oscillator pair mid-gesture), and every allOff() path (mono/poly toggle, engine switch, MIDI all-notes-off, reset, GUI panic) silenced only oscillator 0, which IS a stuck note. Two things generalise beyond the original entry. (4) THE ALIAS IS THE HAZARD: `SwarmCore &core = cores[0]` existed so the multi-instance port would not have to touch every legacy call site, and that convenience is precisely what made eight wrong sites read as correct code — a compatibility alias over a newly-multiplied resource converts "unported" from a compile error into a silent runtime bug, so it should be introduced only with a deadline to remove it. (5) PARTIAL FAN-OUT IS THE TELL: when one operation of a family fans out and its sibling does not, the port was done by call site instead of by family; grep the whole family the moment you find one. | falsifier: a multi-instance system where per-consumer instance binding stayed correct without a shared indirection — or one where role indirection itself caused comparable harm (e.g. a consumer that genuinely needed a pinned instance, such as an A/B comparison view, forced through a single global role handle).

[L0029] Performance gestures enter ONE routing layer; a second hand-wired path for the same signal class is a bug generator | tier: candidate | added: 2026-08-09 | tags: architecture, mpe, modulation, routing, plumbing, doctrine, cross-project, HIGH-PRIORITY | lesson: every performance gesture — velocity, aftertouch/pressure, per-note tuning, channel bend, mod wheel — is a SOURCE and must enter the instrument through the same routing layer as every other source, then be distributed to consumers by that layer. HYPERSAW instead grew TWO parallel paths for the same class of signal: a modulation matrix (sources, destinations, depths) AND a hand-wired MPE path in the event loop that reaches consumers directly. The cost is combinatorial and silent: with E event types and C consumers, hand-wiring is E x C independent chances to miss a connection, and every new consumer re-opens all E. That is exactly how PRESSURE came to fan out to every oscillator while TUNING did not — one row of the matrix was filled in and its neighbour was not, and both compiled. Three consequences worth carrying: (1) the fix is NOT a fan-out helper per operation family (that reduces E x C to E and still scales with event types — HYPERSAW's 2026-08-09 patch, honest but not the cure); it is ONE table where a gesture is routed once and every consumer reads the same routing; (2) a routing layer makes the question "does pressure reach oscillator 2?" UNASKABLE, which is stronger than making it answerable — there is no per-consumer wiring to get wrong; (3) the layer must exist BEFORE the second consumer, because the hand-wired path is cheapest exactly while there is one consumer and is never cheap again (L0027's sequencing lesson, instantiated). | evidence: HYPERSAW 2026-08-09 — eight sites in the CLAP event loop addressed oscillator 0 where the intent was every oscillator (PR #242): bend split the oscillator pair mid-gesture, and every allOff() path left the other oscillators gated, which is a stuck note. Human's framing: "MPE should go to the plumbing and get routed from there instead of messy redundancies and missed connections." | falsifier: an instrument where per-consumer gesture wiring stayed correct across a genuine multiplication of consumers without a routing layer — or one where forcing gestures through the mod matrix cost required immediacy (a per-note tuning that MUST bypass smoothing/depth to stay sample-accurate would be a real counter-pressure, and would argue for a routed FAST LANE rather than for hand-wiring).

[L0030] An ORACLE is the most portable thing a project can donate — it transfers the requirement without the implementation | tier: candidate | added: 2026-08-09 | tags: integrations, oracles, testing, library, doctrine, cross-project | lesson: when a mature project seeds a shared library, the instinct is to donate CODE (modules, headers) or RULINGS (docs). The higher-leverage artifact is the ORACLE: an executable statement of required behaviour that names no internals. A donated module carries the donor's accidents — its allocation habits, its aliases, its scope assumptions — and the library inherits them; a donated oracle carries only the requirement, so the library can build the subsystem from scratch, the way it should have been built, and still be held to behaviour that was paid for in real bugs. HYPERSAW's mpe_check is the worked example: it drives the public plugin interface and measures emitted audio, so it names no oscillator, no core, no alias — it would pass or fail an implementation sharing none of HYPERSAW's code. Corollary: this is only true of oracles written against the PUBLIC surface and observable output. An oracle that reads internal state (a per-voice getter) is a code donation wearing a test's clothes, and it pins the recipient's architecture to the donor's. | evidence: HYPERSAW 2026-08-09 — human, on the MPE routing lesson: "This would be a good candidate for having the library build up from scratch efficiently and then we can test against the oracle." mpe_check drives factory -> activate -> events -> process and detects via Goertzel on the output, deliberately not via a state read (the state_check trap, where a round-trip through one broken accessor agreed with itself). | falsifier: a donated oracle that turned out to encode the donor's implementation rather than the requirement — i.e. a correct-but-different implementation fails it. That is the test of whether it was really an oracle; if it happens, the oracle was over-specified and must be loosened to observable behaviour.

[L0031] A reference oracle certifies AGREEMENT, not correctness — and only over the surface the reference actually SPANS | tier: canonical | added: 2026-08-10 | consolidated: 2026-08-11 | tags: oracles, testing, parity, coverage, architecture, doctrine, cross-project, HIGH-PRIORITY | lesson: parity-to-a-reference is the strongest available check on FIDELITY and is blind in TWO distinct ways, both of which present as a large green suite. (A) IT SHARES THE REFERENCE'S BUGS — it answers "does the port match the sketch?", never "is the sketch right?", so a defect present in both is not merely missed, it is CERTIFIED, and every passing scenario increases confidence in it. (B) IT COVERS ONLY THE SURFACE THE REFERENCE RENDERS, and there are three recurring ways to fall outside that surface: (1) THE SHELL PATH — an oracle that constructs the core DIRECTLY never exercises the integration layer, so a shell-side default divergence is invisible; before letting a new param SHARE an existing shell id, check whether ANY oracle drives the shell->core path at that param's default, and if not, give the second engine its own param with its own reference default so the goldens AND the shipped plugin are reference-exact by construction. (2) THE SUPERSET — a deliberate implementation-only range the reference does not implement is invisible BY CONSTRUCTION; parity passes forever while the superset region does anything at all, so the same commit that ADDS a superset must add an invariant oracle for it. (3) THE LAYER — anything downstream of what the reference renders gets ZERO coverage, and the green is seductive precisely because it is large and unrelated; before offering a passing fidelity suite as evidence for a change, state which layer the reference renders and whether the change is inside it. Three consequences: (i) oracles come in KINDS and a project needs more than one — fidelity (parity/golden), invariant (property checks needing no reference), closed-form; (ii) an invariant oracle outlives the reference and is the most donatable artifact across projects ([[L0030]]); (iii) THE REFERENCE IS A STAGE, NOT A FIXTURE — a prototype-as-spec is a scaffold with a ceiling, and the graduation path is toward references with an independent claim to truth (a high-accuracy integrator, a closed form, a measurement). | evidence: HYPERSAW, four independent instances. (A) consonance gravity integrated once per render call at dt = block length, so output depended on how a buffer was SUBDIVIDED; 147 parity scenarios passed for the entire life of that bug and COULD NOT have failed, since generator and checker both render at a fixed block. A ~40-line property check found it on first run, then found pan motion immediately after (ADR-086, tools/subdiv_check.cpp). (B1) ADR-055: spectra_check builds SpectraCore directly, so sharing the SAW ADSR ids would have pushed SAW's 0.003 s default into an engine whose reference wants 0.004 s — goldens green, shipped plugin diverged; own params 65-68 chosen instead. (B2) ADR-025 super-width: width > 1 is implementation-only, and its negative cross-feed injected phase-inverted saws — 0 cliffs at width <= 1.01, 1414 at width 1.5 — denied repeatedly by me because every check was structurally blind (JS clamps by construction; goldens and waveshape_check all ran width <= 1). The human isolated it by settings-bisection. (B3) 2026-08-11: the 147 goldens render SwarmCore directly, so the whole plugin mix stage (bass-mono, FX rack, master volume) is downstream of them; replacing rack.processStereo with the routing matrix would have shown 147/147 green whether bit-exact or catastrophically wrong, and the actual proof was routing_check assertion 8 (0/1024 samples differ, four active distinct slots). Human's framing: "we would eventually graduate to a better reference once we reached a stage that warranted it, and until that point we can keep coming up with clever ways to test things the reference alone can't". | falsifier: a reference that renders the full signal path end to end AND is independently known correct — then parity does span the change and is legitimate evidence for it. Inverse falsifier for the invariant half: an invariant oracle producing enough false positives to be abandoned, meaning the invariant was asserted more strongly than the domain holds it. Honest limit: invariant oracles only test the invariants someone THOUGHT to state, so they do not subsume adversarial probing. | absorbs: L0011, L0021, L0034 — shell-path, superset and layer blindness respectively; consolidated 2026-08-11

[L0032] A detector that shares an assumption with what it measures will confirm whatever you expect — give every probe a case that MUST read zero | tier: canonical | added: 2026-08-10 | tags: testing, calibration, epistemics, probes, doctrine, cross-project, HIGH-PRIORITY | lesson: the dangerous measurement is not the one that fails, it is the one that AGREES WITH YOUR HYPOTHESIS for a reason unrelated to the hypothesis. Four instances in a single day, each a different mechanism, each producing a plausible number in the predicted direction: (1) a mute probe separated two oscillators by an OCTAVE, so oscillator 1's second harmonic landed on oscillator 2's fundamental and muting read as a 67% leak — the detector's frequency choice shared the signal's harmonic structure; (2) an oscillator-drift probe captured one oscillator 70 blocks after the other, comparing two MOMENTS of an evolving swarm rather than two oscillators — the detector's timing shared the system's evolution; (3) a gate calibration planted its regression with an unasserted string replace that matched nothing, so "the gate passed" was read as evidence when nothing had been planted — the plant shared the silent-no-op failure mode it was testing for; (4) a sample-rate invariance probe sampled every 256 SAMPLES, so its own time resolution tracked the sample rate, manufacturing a 1.4% variation that vanished when resolution was equalised in TIME — the detector's resolution shared the variable under test. THE CONTROL IS THE CURE: include a case that must read EXACTLY zero, and make the probe report it. WHEN THE SUBJECT IS A REPORT RATHER THAN A SIGNAL, the control must be negative: a diagnostic dump is a text file full of plausible-looking lines, and 'the value appears in the file' is satisfied by a file that mentions every value — so each positive assertion pairs with one that must NOT match, on a capture taken from a state where the thing genuinely is absent. That reshaped a real oracle: 'the gated voice is visible' only became meaningful once paired with 'and after release, none' (tools/trace_check.cpp, 2026-08-11). A gravity-off render differing by 0 of 132,300 samples is what proved the rig contributed nothing; the same control, run first, is what exposed (2) by "failing" at 0.84 with nothing to fail about. Corollary for tooling: every mutation in a calibration asserts its anchor, because a plant that silently does nothing and a fix that silently works are indistinguishable from the outside. NECESSARY BUT NOT SUFFICIENT, learned 2026-08-11 by watching a second plant report the FIRST plant's failures verbatim: **asserting the anchor proves the SOURCE changed, never that the BINARY did.** A stale object file makes the calibration measure the previous experiment, and it presents as the most convincing possible result — a plant that 'fires' exactly as predicted. Force the rebuild (delete the object, not just re-run the build system, which may not track a header or a sibling translation unit as a dependency of the test target). Third occurrence of the stale-object half in three different tools. Corollary of the corollary: a plant whose failure is IDENTICAL to the previous plant's, including assertions it should not touch, is a stale binary until proven otherwise — the discriminator is an assertion the new plant CANNOT affect. SHARPENING, 2026-08-18 — the control has a coverage surface too, and it is the same trap one level up. A must-read-zero case run on a corpus YOU wrote can only test assumptions your fixtures do not already share; the assumptions worth finding are precisely the local conventions you were unaware of holding, and those are invisible against your own material. So the cure has two halves, and either alone is the trap: (a) run the detector against a FOREIGN corpus — someone else's real data, not your fixtures — because that first foreign run is the only cheap test of the assumptions you cannot see; and (b) pair the clean read with a CORRUPTION of that same corpus that must read non-zero, or the zero cannot fail. A must-read-zero assertion with nothing able to make it non-zero is a check that cannot fire, which reads exactly like a check that passes. Cheap way to prove (b): neuter the function under test to return the passing value and re-run the suite — anything still green was never exercising it. | evidence: HYPERSAW 2026-08-09/10 — mixer_check (tritone rewrite), the abandoned oscdrift probe, subdiv_check's first calibration, and the sample-rate probe that then found a real +0.42% defect once corrected. ALSO 2026-08-18, and outside our own code: `library-entry.3` added an `absorbs` field to its changelog, its JSON Schema and its quarantine rule but not to the one regex naming which segments OPEN a field, so a conforming parser routed the field to `extra` — and the quarantine rule guarding it could never fire, because the field it guarded could never open. Neither the author (who checked self-consistency) nor the first reader (still on v2) was placed to see it; we found it as the first party to WRITE the field. The contract owner then fixed it, pinned the class mechanically, and shipped `kit/gates/library_validate.py` — whose own first run produced three false positives, all from the detector assuming its local conventions. AND the sharpening's own evidence, 2026-08-18: our correspondent shipped a LIBRARY validator, whose first run against OUR file — the first foreign corpus it had seen — produced three false positives, every one a local-convention assumption that had been invisible against its author's own fixtures. They then measured the second half rather than assuming it: monkeypatching the file-level `validate_file` to return no findings left 12 of 12 tests green, because every other test exercised the entry-level path and never the file-level one, so the corpus-must-read-zero assertion could not fail. Now paired with a corruption of the same corpus and re-proven against the neutered function. | falsifier: a probe whose zero-control passed while the probe itself was still confounded — which would mean the control shared the confound too, and is the reason a control must be chosen to be independent of the mechanism under test rather than merely convenient.

[L0033] A calibration plant that does NOT fire is a finding, not a failed attempt | tier: canonical | added: 2026-08-11 | tags: testing, calibration, oracle, coverage, epistemics | lesson: when you plant a defect to prove an assertion is live and the suite stays GREEN, you have just measured the assertion's COVERAGE BOUNDARY — record it in the source next to the assertion, and do not silently retry with other plants until one fires. The instinct to keep trying is the bug: it converts "I discovered this assertion doesn't check X" into "I eventually found a plant that works", and the boundary is lost. Name explicitly which assertion DOES cover the thing the no-op plant probed, so the division is recorded rather than left to be assumed the other way. | evidence: two independent instances in `tools/routing_check.cpp`. (1) increment 1: the routing lab's terminal-test bug was a no-op because `isTerminal` excludes illegal destinations by its loop bound, not by the legality call — the protection is structural, and claiming the call provided it would have been a comment that lies. (2) increment 2 (2026-08-11): removing the `isTerminal` filter from the terminal sum was a no-op for the new bit-exactness assertion, because `setSerialChain` leaves `outAmount = 0` on non-terminals so summing them adds zeros — that assertion covers gather and aliasing (both plants fired, 1023/1024) but NOT terminal detection, which assertion 4 covers. | falsifier: a no-op plant that, on inspection, is genuinely unreachable in any configuration rather than uncovered — then it is noise about the plant, not information about the assertion. | supersedes: —

[L0035] Correct OUTPUT does not imply correct PROTOCOL — obligations to a consumer are a second observable surface, with its own coverage | tier: canonical | added: 2026-08-12 | tags: oracles, protocol, events, plugins, coverage, epistemics, doctrine, cross-project | lesson: a system that both PRODUCES something and OWES a consumer bookkeeping about it has two observable surfaces, and an oracle suite usually covers only the first. Every check that measures the product — audio, rendered pixels, returned rows — stays green while the obligation surface is corrupted, because the product genuinely IS correct. The failure is invisible in the strongest sense: not merely unmeasured, but unmeasurable by the kind of test the project is full of, and it reaches the consumer as state corruption with no local symptom at all. Ask directly: what does this system PROMISE its consumer beyond the artifact — lifecycle events, acknowledgements, resource release, cleanup callbacks, transactional commits — and is any of it asserted? Every path that can DISCARD an obligation is the specific thing to enumerate; a system with two ways to drop an obligation, one that discharges it and one that does not, will eventually take the second. The structural cure is to make the discharging path the ONLY route (an identity that can leave only through the call that emits its end), so the defect has no spelling. | evidence: HYPERSAW, two NOTE_END bugs a year apart, both invisible to a full green suite because the AUDIO was correct in both. (1) 2026-08-03 (L0022): `emitNoteEnds` ignored `try_push`'s return and retired the tag anyway, so a busy host destroyed an END permanently — four rounds of wrong fixes chasing a stuck note before a REJECTING stub in the harness caught it. (2) 2026-08-11: `panicWithDump` zeroed `pendingEndCount` and cleared every tag, destroying every owed END; all EIGHTEEN gates green, the notes audibly stop, only the host's `note_id` bookkeeping is corrupted. It surfaced solely because an external reviewer asked which END cases were unmodeled and answering honestly required reading the function. Neither was findable by listening. | falsifier: a project whose product-side oracles DO catch an obligation defect — which happens when the consumer's state feeds back into the product (a host that withholds retriggering a leaked pitch, as this one eventually did), so the corruption becomes audible downstream. The rule binds wherever the obligation surface is write-only from the producer's side. | supersedes: — (generalises the symptom class of [[L0022]], which stays as the API-contract rule)

[L0036] PIN YOUR REFUSALS — a deliberate absence needs a test, or it becomes an accidental presence | tier: canonical | added: 2026-08-12 | tags: testing, design, invariants, doctrine, cross-project, epistemics | lesson: systems record what they decided and almost never record what they deliberately did NOT decide. An omission with no assertion behind it is indistinguishable from an oversight, so the next competent person closes it — helpfully, plausibly, and wrongly. Whenever you leave something out ON PURPOSE (a capability withheld, a question left open for a human, a coverage boundary you found rather than closed), write the test that FAILS if someone adds it back. Three shapes recur: (1) a deliberately ABSENT operation — assert that no route to the forbidden state exists, so the absence is structural rather than remembered; (2) a deliberately UNANSWERED question — assert that the currently-illegal-looking case still VALIDATES, so nobody "fixes" the checker into pre-ruling it; (3) a deliberately UNCOVERED region — record the boundary next to the assertion that stops short of it, naming which other assertion does cover it. The general form: documentation of an intention is not enforcement of it, and an intention that only lives in prose is one refactor from being reversed by someone who never read the prose. | evidence: independently arrived at in two repos within days, which is what promoted it. HYPERSAW: L0033's no-op calibration plants — a plant that does not fire measures a coverage boundary, recorded next to the assertion instead of retried until something fires; and `isTerminal()` computed rather than declared, so a manifest must NOT offer an `is_output` field or it becomes a second source of truth for a question the edges answer. FOUNDATIONS (2026-08-12, their DECISIONS #60/#61): a static_assert pinning that a model-A -> model-B -> model-A table PASSES validation, precisely so a later session cannot "fix" the validator into pre-ruling an open human question about feedback semantics; and a NoteTable with deliberately no clear()/reset()/panic(), so that a note identity cannot leave the table except through a path that delivers its END. Their framing of the convergence: two repos independently reaching "pin your refusals". | falsifier: a deliberate absence that stayed absent across substantial refactoring by people who never saw the rationale — then prose sufficed and the test was ceremony. Note the asymmetry that makes this cheap: the test costs one assertion, and the failure it prevents is silent, plausible, and discovered late. | supersedes: — (generalises the discipline in [[L0033]], which stays as the calibration case)

[L0037] A correspondence artifact's frontmatter is a CLAIM about protocol state; the correspondent's git history is the FACT — check the tree before reasoning from the letter | tier: candidate | added: 2026-08-18 | tags: cross-project, protocol, provenance, doctrine, process | lesson: reading another project's mailbox, I answered from a brief whose frontmatter read `status: filed / ball: provider` and produced a full triage of an open question. It had been RULED ten hours earlier; the answer was committed in the same tree, in a sibling file I did not list. The frontmatter was stale because the responder updated the contract and the response but not the question's header — and nothing in the exchange format forces those to agree. THE GENERAL SHAPE: any artifact that carries a status field maintained by hand (exchange frontmatter, a ticket, a README badge, a task table, a roadmap checkbox) asserts a state that the underlying tree can silently contradict, and it fails ASYMMETRICALLY — a stale `open` costs a whole session's work, a stale `closed` merely gets re-checked. THE RULE, which is cheap: before reasoning from a status field, ask the tree the same question — `git log -- <dir>` over the correspondent's thread, or grep for the artifact id in their committed history. The question and its answer are usually adjacent files; listing the directory would have shown it. Corollary for the writing side: a status field nobody gates is documentation, not state — the fix is an assertion that the field and the tree cannot disagree, not a discipline to update it. | evidence: 2026-08-18, triaging autonomous/integrations/distillery/brief-004.md. Frontmatter said filed/provider; `d14fae0` (2026-08-17 20:09) had ruled it and committed `response-004.md` beside it. Cost: one session's triage plus a drafted evidence document, superseded before it was filed — and the recommendation it reached (option (b)) was the ruling that already existed. The correspondent accepted it as their bug (their Decision 56: 'frontmatter is protocol state and the resident owns it'), swept 21 of 23 files by hand — and the hand-sweep then MISSED two, including the thread about the sweep, which is the corollary demonstrated inside its own fix. Now gated as `ball_scan.frontmatter_lies` in their verify, proven to fire on a planted lie (their Decision 57). | falsifier: an exchange format where the status field is generated from the tree rather than maintained beside it — then the field cannot be stale and reading it is reading the tree. Equally: if checking the tree first routinely costs more than the stale reads it prevents, the asymmetry claim is wrong and the cheap read is correct. | supersedes: —

---

## Retired IDs (consolidated 2026-08-11)

IDs are never reused and never renumbered — other entries, traces and ADRs cite them.

- **L0011** (parity oracle constructing the core directly misses shell divergence) → absorbed into **L0031** as blind spot B1.
- **L0014** (sample where the signal ISN'T) → absorbed into **L0016** as its spectral case; L0014 always described itself as L0016's special case.
- **L0021** (parity cannot see superset-only regions) → absorbed into **L0031** as blind spot B2.
- **L0034** (know which LAYER your fidelity oracle spans) → absorbed into **L0031** as blind spot B3; it was filed one day earlier already marked "refines L0031".

Rationale: four entries stated one structural claim — *parity's coverage has a boundary, and the boundary is where the bugs live*. Four near-identical lessons is a retrieval problem, not four lessons: a session hunting this has to find the right one, and any single one under-warns. One canonical entry naming all three ways to fall outside the reference's surface is strictly stronger.


[L0038] Oracle coverage is per-TRANSPORT, and an exclusion needs an owner | tier: candidate | added: 2026-08-19 | tags: oracle, midi, coverage, transport | lesson: Three sessions fixed real bugs around a dead pitch wheel because every oracle drove the core or sent CLAP params, while the DAW delivers the wheel as raw MIDI — a transport no tool in the repo had ever exercised (`grep CLAP_EVENT_MIDI tools/` was empty). The drop itself was a REASONED exclusion (don't read a ±2 st wheel at the ±48 st MPE range) whose excluded case nobody then owned. Two rules: (1) enumerate the transports the host actually uses (params, notes, note expressions, RAW MIDI) and cover each end-to-end at least once; (2) any `break`/`continue` that excludes a case with a good reason must name where that case IS handled, or it is a silent feature deletion. | evidence: traces/2026-08-19-wheel-never-reached-engine.md — probe flat 220 pre-fix, lawful ramp post-fix; mpe_check BND-6 RED/GREEN both directions | falsifier: a transport-complete oracle suite that still misses a dead input path, or an owned exclusion that rots anyway | supersedes: —

[L0039] A check built against one sample inherits that sample's coincidences | tier: canonical | added: 2026-08-22 | tags: oracle, detector, sampling, cross-project | lesson: When a detector is designed against a single observed instance, it silently encodes that instance's ACCIDENTS as if they were the contract, and then passes for the wrong reason. Two independent instances the same week, from two projects: (a) FOUNDATIONS' note_shadow keyed its voice-row grammar on `--`, which was HYPERSAW's INACTIVE-tag placeholder appearing in every row of the first capture only because that capture was post-panic; a second capture with live tags parsed 0 of 11 rows. Their own framing: 'a format designed against one sample inherits that sample's coincidences.' (b) HYPERSAW's enable readback indexed shell state by the base id, so every oscillator mirrored oscillator 1 — and the probe that 'verified' it SET the value first, the one condition under which engine truth and the aliased read coincide. The remedy is the same in both: the detector needs a second sample that differs in the dimension the first one held constant, and that sample must be pinned as a fixture, not just observed once. | evidence: FOUNDATIONS response-panic-dump (12 failures, all theirs, 6th check-that-cannot-fire; both captures now pinned as their 28th ctest) · HYPERSAW ADR-107 truth sweeps (239 ids, 3 lies on first run) | falsifier: a detector whose fixtures vary in every dimension the contract allows and which still passes for the wrong reason — or a single-sample detector that provably cannot encode an accident because the format is externally specified | supersedes: — (extends L0024's must-read-zero control and L0032)

[L0040] Meeting every acceptance criterion is not evidence the change did its JOB | tier: candidate | added: 2026-08-24 | tags: measurement, epistemics, gui, performance, baseline, doctrine, cross-project | lesson: A change commissioned to buy a RESOURCE (space, time, memory, cycles) has two separate questions — "is it correct?" and "did it buy the resource?" — and the acceptance criteria usually only answer the first. Converting 121 slider rows to knobs to make the layout more compact, I verified every SS4 criterion numerically (sweep geometry, drag ratio, shift-fine, bipolar fill from centre, dbl-click reset, disabled refusal) and all of it passed. The change bought 30px of 1483 (2%), and four of five pages were BYTE-IDENTICAL. The cause was arithmetic nobody had done: a knob cell was 78-87px against a 28px slider row, so a cluster only broke even at 4-across and a 5-knob cluster came out TALLER than the sliders it replaced. Every criterion was about the knob; none was about the cell. THE RULE: when a change exists to buy a resource, state the resource as a NUMBER before starting, extract the pre-change artifact from version control (`git show main:<path>`), run both, and diff the number. Not the rendered result — the resource. THE FAILURE MODE THIS PREVENTS is specifically invisible: the work looks finished, demos well, and satisfies its written contract, so nothing prompts the check. Related but distinct from the detector-calibration family (L0016/L0024/L0032/L0039): there a detector confirms a defect for the wrong reason; here every check is honest and correct, and the change still does not do what it was for. | evidence: traces/2026-08-24-knobs-and-density.md. Baseline extracted with `git show main:src/gui/gui2.html`, served, and measured per page. First cut: OSC 1576->1546 (2%), MAIN/MIX/FX/MORPH identical. After attacking the CELL rather than the knob (readout takes the label's slot on hover/focus/drag; unit + scope tag dropped from the face, kept in `title`): cell 78-87px -> uniform 61px, OSC 1576->1395 (-11.5%), labels clipped 28/45 -> 0/39. | falsifier: a resource-motivated change whose acceptance criteria DO bound the resource (e.g. "cell height <= 62px" as a criterion, not just "the knob obeys SS4") — then criteria-satisfaction and purpose-achievement coincide and the extra measurement is redundant. Equally: if extracting and running a baseline routinely costs more than the unmeasured regressions it catches, prefer the criteria. | supersedes: —

[L0041] `typeof x` does NOT guard a let/const -- it throws in the temporal dead zone | tier: candidate | added: 2026-08-24 | tags: javascript, gui, initialisation, guard, epistemics | lesson: The idiom `if (typeof f === 'function') f()` is the standard defensive guard for "call it only if it exists", and it is SAFE for undeclared identifiers and `var` -- which is why it reads as universally safe. It is NOT safe for `let`/`const`: inside the temporal dead zone, `typeof` throws ReferenceError rather than returning 'undefined'. So a guard written precisely to survive early initialisation is itself the thing that crashes during early initialisation, and it crashes in the one ordering the guard existed to handle. Shape to recognise: any function that runs BOTH at module-init time and later on an event, referencing something declared further down the file. The fix is not a better guard -- it is to stop calling the late thing early: pass a flag (`applyScheme(repaint)`) so the boot path skips the work and only the event path does it. THE GENERAL LESSON: a defensive check whose safety you have not verified for the specific declaration kind is not a check, and this one fails silently-looking (a whole page of controls simply never renders). | evidence: 2026-08-24, gui2.html scheme cycler. `applyScheme()` ran at chip-creation time and guarded its repaint with `typeof canPaint === 'function'`; canPaint is a const declared ~200 lines later. Console: 'ReferenceError: Cannot access canPaint before initialization' twice, no tabs rendered at all, so every later probe reported the OSC tab missing. Same class as the two prior TDZ hits in this repo (VPOS in formant-lab, MCOLORS in gui2) -- third occurrence, but the FIRST where a deliberate guard was present and gave false confidence. | falsifier: a JS engine or transpiler target where typeof on a TDZ binding returns 'undefined' (pre-ES6 `var` semantics, or code compiled down to var) -- there the guard genuinely holds and the lesson does not apply. | supersedes: —
[L0042] "the fix didn't work" means FIRST check the fix ever reached what the human tested | tier: candidate | added: 2026-08-25 | tags: verification, install, phantom-test | lesson: The installed plugin is a separate artifact from the built one, and the gap is silent in both directions: a session that builds but never installs (install needs dangerouslyDisableSandbox, so it never happens as a side effect) leaves the human A/B-testing yesterday's binary against today's conversation. Before diagnosing a "failed" fix, diff install vs build mtimes AND grep the installed binary for a marker string unique to the change — with `grep -a`, because BSD grep silently reports no-match on binaries without it (the first probe here read 0 on a binary that contained the marker 3 times, blind in exactly the reassuring direction). The 2026-08-25 report "this fix didn't work at all" was a plugin installed at 11:15 versus a fix built at 18:01. | evidence: traces/2026-08-25-arm-corner-dotted.md follow-up; installed binary had zero markers, built had 3; user report matched the PREVIOUS build's behaviour exactly | falsifier: a "didn't work" report where the installed binary carries the change's marker string — then the fix itself is wrong and this lesson does not apply
[L0043] A subagent's summary is a claim about the tree, not the tree — read the primary source before ACTING on it | tier: candidate | added: 2026-08-26 | tags: delegation, provenance, mailbox, cross-repo | lesson: Delegated recon is legitimate for FINDING things and illegitimate as the basis for an irreversible act. On 2026-08-26 the lead filed a cross-repo answer to `response-seam-round2.md` having read only a scout's report of it — quoting the scout's quotes — and the filing asserted as fact ("the Aug-25 answer was written without sight of the first round") the exact thing the scout had listed under "ambiguities I will not resolve by inference". Reading the source took one command and changed two things: the claim was re-grounded in checkable evidence (zero references to the prior round; it replaced that document in place; filed on the respond-by date), and the correspondent's own premise turned out to be wrong and checkable — they wrote "ADR-083 postdates the first answer" when it landed four days before it (22d0b1d, 2026-08-07). Neither correction was available from the summary, because a summary preserves conclusions and discards the checkable details. The rule: delegate the search, read the document before you answer it. | evidence: traces/2026-08-26-adr126-and-plectrum.md and the amend commit bbf8d1a in the FOUNDATIONS mailbox; the scout's own report flagged the ambiguity the lead then asserted | falsifier: a case where reading the primary source after acting on a competent summary changes nothing material, repeatedly — then summaries are sufficient for action and this lesson is overcautious. One counterexample is not enough; the cost here is asymmetric because the act was outbound and irreversible-by-default.
