# Day plan — 2026-08-28, second half (authored for the Opus lead)

> **Origin:** HYPERSAW lead session (Fable), 2026-08-28, at the human's request:
> *"I'm going to switch to Opus shortly; please take care of as much of the
> long-horizon thinking as you can for the rest of the day's steps."* This
> document front-loads design and decision framing so the executing session
> implements rather than re-derives. Motivating state: PRs #492–#498 merged
> today (mod increments 1–6, CHROME-001 v1→v2, ADR-132..140). ROADMAP.md
> remains the single source of truth; if this plan and ROADMAP disagree,
> ROADMAP wins.

## Mailbox: CLOSED — do not re-derive this

All three "unread" FOUNDATIONS threads were read this session. Every one ends
**ball: none**: `response-stage3-answers-taken.md` (they took all three of our
routing answers into their manifest boundary; F2 Stage 3 closed),
`response-seam-round2` (settled 2026-08-26 by our own answer: the Aug-11 round
governs, no work either side), round1 (our ack landed, map revised same-day).
**Nothing is owed in either direction.** One fact worth reusing: FOUNDATIONS
formally recorded that any future graph facility must carry per-edge gain,
node constants, and an ordering — that is exactly B50's visual-routing-matrix
contract, so cite their DECISIONS #61 when that build starts.

---

## Execution order for the rest of the day

1. **B71 — table-side route UX** (small, fully specified below; matrix lane)
2. **B73/B68 increment A — the standard Delay module** (FX lane, biggest item)
3. **B72 — put the morph-transition decision to the human** (framing below;
   do NOT build until ratified)
4. Optional if the day runs long: B73 increment B (Echo regen fix — design
   final below, small diff)

Items 1 and 2 have disjoint file scopes and could be parallel dispatches, but
serial is fine. Everything below B72 waits on human rulings.

---

## 1 · B71 — table-side route UX (design final; build it)

**The ruling being implemented** (human, verbatim): *"it should just be 'send
to mod matrix' and it sends it to a table and from there you can decide which
modulator to link it to (or x to release it)"* + *"a 'release all modulators'
option for params sent to the mod matrix."*

**Design decision — no "pending/unlinked" state.** A sent param immediately
becomes a real route with **default source ENV 1, default depth 0.25**; the
MOD-page row grows a **source `<select>`** (ENV 1 · ENV 2 · Macro 1–8) so "you
decide which modulator" is a re-aim of a live row, not a new lifecycle state.
Rationale: a pending-unlinked row is a second route representation that needs
its own persistence, refusal rules, and halo semantics — all cost, no audible
gain. Rejected alternative recorded here so it isn't re-invented.

**Shell (src/hypersaw_clap.cpp):**
- `void modSetSource(int idx, uint32_t src)` — bounds-check idx, src <
  kMaxSources, set `mod.routes[idx].src = src`. The pitch route (synthetic
  dest) is legal to re-source in principle but the GUI never offers it —
  guard anyway: refuse if `routes[idx].dest & kModDestSynthetic` (its source
  is ADR-135's contract).
- hostIf lambda + `hzModSetSource` bind (gui.h + gui_common.h, copy the
  modSetDepth pattern).

**GUI (src/gui/gui2.html):**
- PARAM_MENU: DELETE `mod1`/`mod2`/`modMac`, ADD:
  - `modSend` — show: bridge present, `el.type==='range'`, id not 161–178,
    **and no existing route to this dest** (`!MODROUTES.some(r=>r.dest===id)`
    — a second route to the same dest is created from the table later if ever
    needed; B72's canonical (src,dest) identity argues one row per pair).
    Label "Send to mod matrix"; hint "starts on ENV 1 — re-aim it on the MOD
    page". Run: `hzModAdd(0, id)` then `renderModRoutes()`.
  - `modRelease` — show: `MODROUTES.some(r=>r.dest===id)`. Label "Release all
    modulators". Run: collect this dest's route indices from MODROUTES, call
    `hzModRemove` **in descending index order** (removal compacts — ascending
    order removes the wrong rows; this is the same index-shift trap as the
    161 landmine), then `renderModRoutes()`.
  - MODROUTES may be stale on first right-click after load (it fills on MOD
    render / halo tick). Refresh it in the contextmenu handler before
    building `ctx` (an `await hzModRoutes()` there is cheap and makes the
    show-guards truthful).
- renderModRoutes: replace the static `ENV 2 → ` label prefix with a
  `<select>` of MOD_SRC_NAMES; `change` → `hzModSetSource(r.i, +sel.value)`;
  pitch route's select is disabled (its source is the knob's contract).
- ADR-141 records the menu shrink and the no-pending decision, superseding
  ADR-137's in-place macro submenu (that submenu code is deleted, along with
  the `keep:true` mechanism if nothing else uses it).

**Acceptance:** right-click any routable knob shows exactly "Reset to
default" + "Send to mod matrix" (or "Release all modulators" when routed);
send creates ENV1@0.25 visible in the table; re-aiming the source to Macro 1
and moving M1 modulates (verifiable headlessly: `hypersaw_test_mod_add(0,4)`,
then modSetSource via a new test hook if added, else in-browser); release-all
on a 2-route dest removes both and the halo clears. Verify: `./verify fast`,
parity 156/156, in-browser menu check (the ADR-136 stub-bridge pattern —
serve `src/gui/gui2.html`, stub `hzModRoutes`/`hzModAdd`). Update test rows
B69-* that name the old menu items. **Persistence note:** re-sourcing changes
the canonical (src,dest) key; that is correct — the chunk serializes whatever
stands.

**Out of scope:** B70 depth-mod, per-note fan-out, any FX file.

---

## 2 · B73/B68 increment A — the standard Delay module (design final)

**Why this shape.** The human's complaint — *"Echo and room need feedback,
and the feedback from their lab didn't really work very well"* — is DIAGNOSED
(read `src/time_core.h:120-190` before touching anything):

1. **The `/N` feedback norm crushes loop gain.** `fbSig = wetRaw / n` is the
   worst-case-correlation law; with N≈8 decorrelated taps the audible loop
   gain at regen 0.9 is ≈0.32. Repeats die in 2–3 generations and the
   edge-of-oscillation zone — the musical heart of a delay — is unreachable.
2. **Mean-collapse smears.** All taps average into ONE write head, so every
   generation re-smears through the whole tap swarm: wash, not repeats.
3. **`tanh` at unity in-loop + always-in-loop damping** duck and dull every
   pass.

Room's FDN (negated Householder) is structurally correct; its problem is only
that `regen` maps to decay time perceptually cramped (the human's own tail
measurements: −102 dB at 0.3 vs −34.8 dB at 0.97 — the action all lives above
0.9). So: the fix is NOT re-plumbing the lab law (B73's roadmap entry is an
explicit license to diverge). Build the **standard delay the human described
for B68** with textbook feedback — it is the A/B baseline they asked for and
almost certainly the keeper — then fix Echo/Room against it (increments B/C).

**Module: `Delay` (slot type 9) in a new `src/delay_core.h`** — framework-free
per doctrine, spec-in-comments since no lab exists for it (record THAT in the
ADR: this is the first engine-side module without an HTML reference; its
oracle IS the spec — impulse-response numbers below are the acceptance
contract, ACCEPTANCE.md gains a section).

State: two delay lines L/R (`kDBuf = 1<<17` doubles, preallocated like ebuf),
one write head each, damped one-pole LP + DC-block per channel in the loop.
No RNG anywhere (no unseeded-RNG blocker by construction).

Per-sample (channel pair, stereo in/out — note the rack is currently mono
per-slot; if `FxRack` slots process mono, v1 collapses to mono in / equal-
power stereo out like Echo does, and ping-pong becomes cross-feed of two
internal lines; check `fx_rack.h` FIRST and follow its seam):

```
readL = lerp(bufL, wL - dL);  readR = lerp(bufR, wR - dR)
fbL   = readL*(1-x) + readR*x          // x = crossfeed 0..1 (ping-pong at 1)
fbR   = readR*(1-x) + readL*x
loopL = dc(lp(fbL));  loopR = dc(lp(fbR))       // damping INSIDE the loop
bufL[wL] = in + loopL*fb;  bufR[wR] = inR + loopR*fb
out   = read (pre-feedback tap), mixed by the rack's existing dry/wet
```

- `fb` range **0..1.08**, soft-limited: `if (|loop*fb| > 1.2) soft = tanh` —
  saturation engages ONLY above threshold, so clean repeats stay clean and
  >1.0 self-oscillates into a controlled ceiling instead of exploding. This
  is the audible fix for lab-cause #3.
- Delay times: `timeL` in ms (log control 1..2000) **or synced** — reuse the
  ADR-103/ id-23 grid-step machinery (`snapGridStep`) for note fractions;
  param `sync` stepped 0/1, `timeSyncL` in beats. Host tempo already reaches
  the shell (the time engines use it — follow their transport read).
  `offsetR` as a ratio (0.5..2.0, default 1.0) per the human's "L/R offsets".
- Time changes SLEW like tSm does (`+= (target-t)*0.0015` per sample — tape
  chirp, no clicks); document that zipper-free retime is a feature, not
  smearing.
- Params (8-block at 232.. next free rack block; check ADR-131's arithmetic —
  four blocks of 8 end at 231, so Delay claims 232-239): time, sync,
  timeSync, offsetR, feedback, crossfeed, damp, (mix lives in the rack's
  existing per-slot mix 133-136). That is 7 — one spare for the in-line HP
  the human requested ("in-line filter"): `loopHp` 20..500 Hz one-pole.
- Mode enum (std / M-S): DEFER. The human listed "std/M-S/ping-pong";
  crossfeed=1 IS ping-pong; M-S needs a stereo rack seam — name it in the
  ADR as deferred-with-reason rather than silently dropping it.

**Oracle: `tools/delay_check.cpp`** (goes into ./verify — remember the
human gate on editing ./verify's gate list: ASK before wiring it in, land
the binary regardless). Impulse-response invariants, all deterministic:
- Repeat spacing: impulse at t=0, fb=0.5, time=100ms → peaks at 100/200/300ms
  ±1 sample after slew settle; peak ratio per generation = fb×(damp gain at
  the LP cutoff) within 1e-3.
- fb=0 → exactly one repeat then silence (< 1e-9 after 2×time).
- fb=1.05 → bounded forever (max |sample| < 1.3 over 30 s) — the soft-limit
  law.
- crossfeed=1 → impulse alternates channels per generation (ping-pong proof).
- Sync: at 120 BPM, timeSync=0.25 beat → 125 ms spacing exactly.
- NaN watchdog heal (copy time_core's pattern + its heal test).
- **Parity-safe superset:** slot type default remains 0/off; parity_check
  156/156 byte-identical with no Delay slot selected.

**GUI:** the rack page already generates per-slot controls from the
presentation table (ADR-131 pattern) — add the 8 rows (page FX, group Delay),
`gen_gui_controls` regenerates, gui_reach/test_table rows per the B69-*
precedent (agentic row cites delay_check once wired, else parity_check;
human row: the B68 A/B listening question, quote the human's "random
distribution laws are barely audibly different").

**ADR-142** (or next free): the diagnosis table above, the license-to-diverge
cite, the no-lab/oracle-is-spec position, deferred M-S with reason.

**Out of scope:** touching time_core's Echo/Room (that is increment B/C),
the FX matrix rework (B50 waits for the visual routing matrix), any mod work.

---

## 3 · B73 increments B/C — Echo/Room fixes (design done; build after A)

**B — Echo:** replace the mean-collapse feedback with **center-tap
regeneration**: `fbSig = (tap at swarm-center delay)` — the same centerIdx
concept the swarm engines already own — with **no /N** (single-tap loop gain
= regen×dampGain < 1, stable by the same argument as the standard delay),
keep DC-block + damp placement. The swarm taps then DECORATE each discrete
generation instead of dissolving it. One-line ADR framing: "the swarm is the
diffusion; the feedback is the meter." Oracle: extend time_check with an
impulse test — post-fix, an impulse at regen 0.7 must show a periodic peak
train at the center delay (was: monotone wash; capture the before numbers in
the trace as the measured justification).

**C — Room:** keep the FDN untouched; re-map the knob to **RT60 seconds**
(log 0.15..25 s): `g = pow(10, -3 * meanDelaySamples / (RT60 * sr))` computed
at control tick. The knob becomes perceptually linear-ish; the FDN math is
unchanged so time_check parity for regen-expressed scenarios must either pin
the OLD mapping via raw-g test entry points or re-measure — **do not weaken
the gate silently; if golden numbers change, that is a re-measurement with
the human informed (charter rule).**

---

## 4 · B72 — the morph-transition question (FRAME, do not build)

Put to the human, once, with this recommendation:

> B72 needs one ruling before routes can join the morph. When corners share a
> link (same source→dest), its DEPTH interpolates like any parameter — that
> part is settled by the canonical key. The open case is a link present in
> SOME corners only. Three options:
> **(a) depth-fades** — absent corners contribute depth 0; the link always
> exists, its strength follows the field (matches the weighted-ADOPT rule you
> ratified for silent-osc params; no clicks by construction).
> **(b) ARGMAX flip** — the link exists only when a corner that has it wins
> the Gumbel draw (ADR-125's topology law; treats a route as structure).
> **(c) exempt** — routes ignore the morph until both… all corners define
> them (safest, least musical).
> **Recommendation: (a)**, on the argument that a route's EXISTENCE is not
> audible — only its depth is — so it is intensity, not topology, and
> intensity interpolates. ARGMAX stays for the SOURCE: if two corners route
> the same dest from different sources, source flips at the boundary (a
> source IS topology). One composition rule: under B70, the depth being
> interpolated is the BASE depth; depth-modulation applies on top.

If ratified, the build is: per-corner depth vector keyed by (src,dest) in the
morph chunk (schema note), morphStep writes depths through `modSetDepth`
under a guard mirroring `morphFromField`, exempt flag per link. Not today
unless the ruling arrives early and item 2 is done.

---

## 5 · Parking / do-not-start

- **B70 depth-mod:** design already in ROADMAP (kDestRouteDepth key space,
  two-pass evaluate, one-level v1). Blocked behind B71 only in the sense
  that the route table UI should settle first.
- **Per-note fan-out:** pitch-only first (per-voice pitch offset from
  per-voice ENV1 — the voice loop already computes env per voice); generic
  per-voice dests need per-voice param shadowing, which is a design, not a
  diff. Do not improvise it.
- **B75 native GUI:** investigation doc only, and only if the human's
  specimen-toggle verdict (test row B74-4) says the webview is still
  untenable. Questions live in the ROADMAP row.
- **B50 FX matrix:** waits for the human's per-module interface visions;
  first build is the VISUAL ROUTING MATRIX (their explicit order). Cite
  FOUNDATIONS DECISIONS #61 on edge-gain/node-constant/ordering when it
  starts.

## Process notes for the executing session (hard-won today)

- Merge stacked PRs bottom-up or the top lands on a corpse (L0044; #493/#494).
- Background browser-pane tabs lay out at ~24 px wide — front the tab before
  trusting ANY measurement (cost 15 minutes today).
- `getContext('2d')` locks a canvas's context type forever — the theme-clear
  loop must keep skipping `chromeC`, and any future GL canvas needs the same
  exemption.
- The EMA throttle counts only real frame times; gaps > 250 ms are excluded.
  Do not "fix" that guard away.
- Every install: `./install` with `dangerouslyDisableSandbox`, then auval;
  identifiers don't survive compilation (L0042) — verify via behavior.
- End every response with the FOUNDATIONS roundup (standing convention).
